# Sprint 2 — Informes

**Status:** ✅ COMPLETO (8/8)

| Ticket | Status | Descripción |
|--------|--------|-------------|
| T-019 | ✅ | Módulo Informes MVP (migration + UI base + 5 tipos) |
| T-020 | ✅ | Editor de contenido + generación con Claude API |
| T-021 | ✅ | Templates parametrizados RGRL (primer form-driven generation) |
| T-022 | ✅ | Templates parametrizados 4 tipos restantes (capacitacion/relevamiento/accidente/otros) |
| T-022.5 | ✅ | Migración Vercel → VPS Hostinger + EasyPanel + Docker |
| T-023 | ✅ | Export PDF con Puppeteer headless |
| T-025 | ✅ | Streaming de generación IA en EditorView (SSE) |
| T-024 | ✅ | Imágenes/adjuntos + logo consultora (cierra Sprint 2) |

## T-019 ✅ Módulo Informes MVP

Primer ticket del módulo de negocio, sienta patrón forward para Clientes/Empleados/EPP/Calendario. Migration `20260511232802_informes.sql`: tabla `public.informes` (`tipo` text+check con 5 valores · `titulo` · `contenido` nullable · `status` `draft|published|archived` · `created_by`) + 2 indexes + 3 RLS policies (SELECT/INSERT/UPDATE con WITH CHECK) usando helpers de T-015 + sin policy DELETE (default-deny para authenticated; UI usa `status='archived'` como soft-archive) + función `audit_informes()` (security definer) con triggers AFTER INSERT/UPDATE/DELETE que escriben a `audit_log` con diff jsonb en `before_data`/`after_data`, cumpliendo la promesa de `tenancy.sql:237-238`. UI MVP bajo route group `(app)`: `/informes` (lista + empty state), `/informes/nuevo` (RHF + zodResolver + shadcn Select), `/informes/[id]` (placeholder de contenido — editor llega en T-020). `createInformeAction` con discriminated union. Sidebar item Informes pasa de `soon` → `live`. Tests: 21 integration RLS/audit/check + 3 integration actions con session cookie mockeado + 2 E2E happy path + 1 nuevo unit en `AppSidebarNav.test.tsx`. Suite total: **40 unit/component + 81 integration + 30 E2E = 151 tests verdes**.

## T-020 ✅ Editor de contenido + generación con Claude API

Primer hit productivo a Anthropic. Anthropic SDK 0.95.1 (singleton server-only en `src/shared/ai/anthropic.ts`, modelo `claude-sonnet-4-6` por ADR-0003). 5 system prompts hardcoded en `src/shared/ai/prompts/` por tipo de informe (relevamiento/capacitacion/rgrl/accidente/otros), cada uno con header común (rol HyS AR, regla PII Ley 25.326, anti-invención de cuantitativos/normativa) + secciones específicas. `generateInformeContentAction` con discriminated union de 9 codes (INVALID_INPUT/UNAUTHENTICATED/NO_CONSULTORA/FORBIDDEN/NOT_FOUND/RATE_LIMITED/CONTENT_FILTER/TIMEOUT/INTERNAL_ERROR), prompt caching `ephemeral` sobre system block, `max_tokens: 4096` por Vercel Hobby 10s timeout (#26 sube a 8192 con Pro tier), logger nunca registra prompt ni response — solo metadata. `updateInformeContentAction` con permission gate defensivo + RLS WITH CHECK del lado DB. Migration `20260512003301_audit_informes_include_contenido.sql` extiende `audit_informes()`: diff guard ahora incluye `contenido`, payload jsonb agrega `contenido_size` + `contenido_preview` (500 chars + `...`). UI `/informes/[id]/editar` con `EditorView` client (RHF + zodResolver, state machine idle/generating/generated/saving, textarea userPrompt con cap 2000 chars + contador, botón "Generar con IA", preview markdown live), `MarkdownPreview` server reutilizable (react-markdown + remark-gfm + rehype-sanitize). `/informes/[id]` ahora renderiza markdown real con botón Editar condicional (creator OR owner). shadcn `textarea` instalado. Tests: 9 integration con `vi.mock('@/shared/ai/anthropic')` (preserva error classes reales del SDK) + 2 E2E (save flow + permission gate UI) + 1 unit nuevo en `env.test.ts`. Follow-up `#26` T-020-FU1 (`tech-debt`, `T-020-followup`). Suite total: **41 unit/component + 90 integration + 32 E2E = 163 tests verdes**.

## T-021 ✅ Templates parametrizados RGRL

Primer **form-driven generation con domain intelligence**. Migration `20260512015104_informe_metadata.sql`: tabla `public.informe_metadata` (1:1 con `informes` via PK=FK `on delete cascade` · `data jsonb`) + 3 RLS policies (SELECT/INSERT/UPDATE) con `EXISTS`-subquery contra `informes` y helpers de T-015 (gate creator OR owner) + función `audit_informe_metadata()` (security definer) con guard `pg_column_size(data) <= 4 KB` y fallback `{_truncated: true}` sin `_field_count` innecesario. Módulo shared en `src/shared/templates/rgrl/`: schema Zod de 14 fields (`razon_social`, `cuit`, `domicilio`, `localidad`, `provincia` enum 24 valores, `actividad_principal`, `codigo_ciiu` opcional, `cantidad_empleados`, `distribucion_turno` enum, `modalidad_operativa` enum, `art_contratada`, `servicio_hys_modalidad` enum, `areas_relevadas` array min 1 max 20, `riesgos_pre_detectados` opcional, `fecha_relevamiento`) + 5 constantes (PROVINCIAS_AR, DISTRIBUCION_TURNO, MODALIDAD_OPERATIVA, SERVICIO_HYS_MODALIDAD, AREAS_RELEVADAS_PRESETS) + helpers `normalizeCuit` / `normalizeRgrlMetadata` + `renderRgrlMetadataAsPromptContext` con sanitización anti-prompt-injection (escape backticks + blockquote para riesgos + footer re-anclaje) + `RgrlMetadataForm` (client RHF, 5 secciones responsive `md:grid-cols-2`, checkbox group iterando presets + textarea "Otras áreas" con dedup case-insensitive cap 20, `<Input type="date">` nativo, `normalizeCuit` onBlur) + `RgrlMetadataSummary` (client con Collapsible "Ver datos completos" + `useMediaQuery` SSR-safe con `useSyncExternalStore`). `generateInformeContentAction` extendido: si `tipo='rgrl'` y hay metadata, prepende al **user message** (system block intacto → prompt caching ephemeral preservado), fallback no bloqueante a comportamiento T-020 si metadata no parsea (drift). `updateInformeMetadataAction` nueva con UPSERT por `informe_id`. `createInformeAction` acepta `metadata` opcional, no bloqueante si persist falla. UI `/informes/nuevo` rewritten a wizard 2-step (`AlertDialog` "Crear sin datos" con label "Crear vacío"), `/editar` suma panel Collapsible arriba con form RGRL + acción separada "Guardar datos", `/[id]` renderiza summary arriba del markdown cuando hay metadata. 3 shadcn components nuevos (`alert-dialog`, `collapsible`, `checkbox`) + 1 hook `useMediaQuery` en `src/shared/lib/`. Tests: 5 integration `informes-metadata-actions` (INVALID_INPUT/UNAUTHENTICATED/NOT_FOUND/FORBIDDEN/UPSERT + audit) + 5 integration `informes-metadata-rls` (SELECT cross-tenant denied + SELECT permitido owner + INSERT denied member-non-owner + UPDATE denied cross-tenant + cascade DELETE) + 4 nuevos en `informes-content-actions.test.ts` (inyecta context + shape end-to-end + combina notes + fallback schema-drift) + 2 E2E `informes-rgrl-template.spec.ts` (wizard happy path + permission gate UI sobre summary). Smoke productivo: output del LLM con todos los valores del form inyectados, placeholders `[A COMPLETAR]` solo en campos no provistos, domain knowledge correcto (Decreto 1338/96, 351/79, WBGT, LOTO, audiometrías), Claude detectó inconsistencia provincia ↔ localidad y la corrigió con nota al firmante. Suite total: **41 unit/component + 104 integration + 34 E2E = 179 tests verdes**.

## T-022 ✅ Templates parametrizados 4 tipos restantes

Completa el módulo Informes con form-driven generation cross-tipo. 4 schemas Zod nuevos en `src/shared/templates/<tipo>/schema.ts`: `capacitacion` (10 fields: 3 cliente + fecha + modalidad enum + duracion_horas decimal + tema_principal + capacitador_nombre + capacitador_matricula opt + cantidad_asistentes + contenidos_resumen opt), `relevamiento` (8 fields: 5 cliente+sitio + fecha + areas_relevadas array + agentes_a_relevar enum array de 10 valores HyS + equipos_medicion opt), `accidente` (12 fields: 3 cliente + fecha + hora HH:MM + lugar + puesto + tipo_lesion array Anexo I Res. SRT 1604/07 + partes_cuerpo array + gravedad enum + dias_baja opt + testigos boolean + descripcion_inicial min 10 max 4000), `otros` (4 fields wildcard: razon_social + cuit + tema_informe + objetivos opt). Módulo `common/` nuevo con `commonClientFields()`/`commonClientFieldsWithSite()` factories + `sanitizeField`+`renderAsBlockquote` + `normalizeCuit` + `PROVINCIAS_AR`+`provinciaName` + `AREAS_RELEVADAS_PRESETS` + `fechaIsoField`+`HORA_HHMM_REGEX` + `summary-ui.tsx` (`Item`+`StatusBadge`+`formatFecha`). 4 renders markdown en `<tipo>/render.ts` con footer de re-anclaje específico por tipo (accidente: NO inventar causa raíz ni testigos; relevamiento: umbrales SRT Decreto 351/79 Anexo V + Res. 295/03; capacitación: no inventar listado nominal; otros: adapta a tema+objetivos sin estructura impuesta). 4 `<Tipo>MetadataForm` client + 4 `<Tipo>MetadataSummary` client siguiendo patrón canónico T-021 (grid 2 cols desktop, Separator+h3 por sección, RHF FormField, Collapsible "Ver datos completos"). **Registry pattern split**: `registry/server.ts` (schemas+renders+normalize, importable desde Server Actions sin arrastrar JSX) + `registry/client.tsx` (defaults+FormComponent+SummaryComponent, bundle visual). `updateInformeMetadataInputSchema` → **discriminated union** `z.discriminatedUnion('tipo', [...])` con 5 variantes; el action verifica `input.tipo === informe.tipo` server-side. Wizard `/informes/nuevo` refactor con `useFormsByTipo()` hook que instancia 5 `useForm` hardcoded (React Rules of Hooks no permite Object.fromEntries+map) — volver al mismo tipo preserva values (UX preferida). `EditorView` refactor: `FormComponent` dinámico del registry; `useForm<FieldValues>` con `SCHEMA_BY_TIPO[tipo]` casteado a `ZodType<FieldValues, FieldValues>`. `/informes/[id]/page.tsx` refactor: `SummaryComponent` dinámico. `getInformeMetadata(supabase, id, tipo)` genérica con return type discriminado `{ tipo, data: unknown } | null`. **RGRL adopta `commonClientFieldsWithSite`+`fechaIsoField`** sin breaking (jsonb keys idénticas, smoke check verde contra 53 filas reales en remote). Tests: 4 unit `templates-<tipo>-schema.test.ts` (~7 tests c/u con happy path + validación bounds + normalize + render sanitize) + 5 nuevos integration en `informes-metadata-actions` via `describe.each(tipoFixtures)` (4 happy path UPSERT por tipo + 1 mismatch tipo) + 2 E2E `informes-capacitacion-template.spec.ts` (wizard happy path + save metadata). Ajuste de `informes.spec.ts` (T-019) al wizard 2-step (los 5 tipos ramifican ahora, no hay "quick path"). Smoke productivo en preview con los 4 tipos nuevos: **outputs truncated por `max_tokens=4096`** (capacitación corta en Anexo D, accidente en sección 7 hallazgos, relevamiento en tabla 4.3 ergonomía, otros en sección 5.4.2). Calidad excelente hasta el cut — follow-up T-022-FU1 (`#36`, cierra automáticamente con T-022.5 migración VPS). Follow-up T-022-FU2 (`#37`, flaky retry pre-existente T-020). Suite total: **68 unit/component + 109 integration + 36 E2E = 213 tests verdes**.

## T-022.5 ✅ Migración Vercel → VPS Hostinger + EasyPanel + Docker

Migración deployment Vercel → **VPS Hostinger + EasyPanel + Docker** — destraba el cap `max_tokens` (sin timeout de plataforma) y baja USD 20/mes vs Vercel Pro. PR `#39` merge SHA `f61aec2`. ADR-0007 con config literal del Service para EasyPanel UI. `next.config.ts` `output: 'standalone'` (imagen Docker ~350 MB vs ~1.2 GB). `Dockerfile` multi-stage Node 22 alpine + corepack `pnpm@11.0.9` + user no-root, **deps stage usa `pnpm install --frozen-lockfile --ignore-scripts` + `pnpm rebuild sharp esbuild`** (workaround dual: bypass `ERR_PNPM_IGNORED_BUILDS` de pnpm 11 strict mode + husky `prepare` postinstall fail por `.git` excluido en `.dockerignore`). `actions.ts` sube `max_tokens` 4096 → 8192 (cap teórico Sonnet 4.6 = 64 k). Deploy operacional: click manual "Implementar" en EasyPanel UI tras cada merge fue el workflow inicial (v2.30.0 self-hosted, probamos 2 approaches automáticos antes de aceptar manual). **T-022.5-FU3 ✅ Auto Deploy de EasyPanel habilitado posteriormente** — push a `main` dispara deploy automático vía webhook GitHub sin intervención. Vercel queda como **hot backup 4 semanas** con auto-deploy pausado, decommission programado +28 días (tracking T-022.5-FU2). Cierra issues `#26` (T-020-FU1) + `#36` (T-022-FU1). Suite total mantiene **213 tests verdes** (cambios sin impacto de runtime); CI ya no tiene job `deploy` (lo dispara EasyPanel del lado del VPS). Smoke productivo end-to-end ✅ con los 5 tipos generando outputs completos sin truncar. **URL productiva: <https://consultora-demo.test-ia.cloud>**.

## T-023 ✅ Export PDF con Puppeteer headless

Export PDF de informes con Puppeteer headless — primer feature de "output" del módulo Informes. Endpoint `GET /api/informes/[id]/pdf` con auth + permission gate (cualquier member de la consultora exporta — mismo gate que SELECT, exportar es lectura), validación UUID del path param, gate de contenido vacío (422 `EMPTY_CONTENT`), audit log `informe_exported_pdf` via service-role (non-blocking — el user ya pagó la generación, perder el log es menos malo que perder el PDF), headers RFC 6266 (`filename*` UTF-8 + `filename` ascii) + Cache-Control private + X-Robots-Tag noindex. Módulo `src/shared/pdf/`: `browser-pool.ts` con singleton lazy de Chromium (idle timeout 5min, HMR-safe via `globalThis` para token efímero `crypto.randomBytes(32)`, SIGTERM/SIGINT cleanup hooks, flags `--single-process` + `--no-sandbox` + `--disable-dev-shm-usage` acordes con alpine + user no-root); `render.ts` con `htmlToPdf()` wrapper (timeouts en cascada: setContent 10s + page.pdf 15s + AbortController 20s hard cap, `PdfRenderTimeoutError` mapeable a HTTP 504); `filename.ts` con `slugifyTitulo()` NFKD + drop diacritics + cut en boundary de palabra y `buildPdfFilename()` formato `informe-<tipo>-<slug>-YYYY-MM-DD.pdf`. Vista print en route group `(print)/` **sibling de `(app)/`** — única forma Next.js de bypasear AppShell (nested layouts componen, no reemplazan); layout valida `x-internal-pdf-render` token efímero, sin match → `notFound()`. `PrintTemplate.tsx` server component reusa `MarkdownPreview` con CSS print inline. `DownloadPdfButton.tsx` client con state machine `idle | downloading`, Blob + `URL.createObjectURL` + anchor.click trigger, parseo Content-Disposition `filename*` UTF-8 con fallback ascii, toasts sonner por código de error (UNAUTHENTICATED con CTA Iniciar sesión, EMPTY_CONTENT warning, RENDER_TIMEOUT, NO_CONSULTORA, genérico); disabled + Tooltip "Generá contenido antes de descargar" cuando informe vacío. **Dockerfile** stage runner: `apk add chromium nss freetype harfbuzz ttf-freefont ttf-dejavu font-noto` + env vars Puppeteer apuntan a `/usr/bin/chromium-browser`. Imagen Docker **~350 MB → ~600 MB** (Chromium + fonts). `next.config.ts` suma `serverExternalPackages: ['puppeteer-core']`. `package.json` suma `puppeteer-core ^23.11.1` (no `puppeteer` full — Chromium se instala via apk en el container, evita descarga npm de 170MB). Tests: 12 unit `pdf-filename.test.ts` + 6 integration `pdf-route-auth.test.ts` + 2 integration `pdf-route-audit.test.ts` + 2 E2E `informes-pdf-export.spec.ts`. PR `#43` merge SHA `2d595b2c`.

**Polish post-deploy** (PR `#48` merge SHA `52a6004`): márgenes generosos para impresión física A4 (22→25mm top, 18→22mm sides, 24→38mm bottom) + footer `font-size: 6.5pt` + `max-height: 15mm` + `overflow: hidden` + bloque `@media print` con page-break controls (`h1/h2/h3 page-break-after: avoid`, `ul/ol page-break-inside: auto`, `li orphans/widows: 2`, etc.) + `body padding-bottom: 10mm` buffer extra. `.pdf-root` padding pasa a 0 (eliminé duplicación con margen Puppeteer/@page que causaba asimetría página 1 vs resto). Smoke productivo validado en `consultora-demo.test-ia.cloud` con RGRL Smoke de 15 páginas: **NINGUNA página con overlap body↔footer + márgenes cómodos para impresión A4**.

**Follow-ups abiertos**: `#44` T-023-FU1 (mem_limit 1g en EasyPanel), `#45` T-023-FU2 ✅ (`resolveInternalBaseUrl` extraído a helper compartido; en `NODE_ENV=production` devuelve loopback IPv4 por default).

**T-023-FU4 ✅ refactor + fix CSS load para PDF print** — bug visual de página 1 resuelto en 2 capas. **(a) Server/client split del SummaryComponent**: el `<CollapsibleTrigger>` shadcn + `<ChevronDown>` lucide quedaban inertes en cada PDF (los 5 tipos) porque PrintTemplate consumía `<Tipo>MetadataSummary` client. Refactor con 5 sibling Server Components nuevos `src/shared/templates/<tipo>/<Tipo>MetadataSummaryContent.tsx`. **(b) Fix `<base href>` para que Tailwind cargue en PDF**: post (a), smoke visual reveló que el grid colapsaba a 1-col. Diagnóstico: `<link rel="stylesheet" href="/_next/static/chunks/...">` usa URL relativa y Puppeteer `page.setContent(html)` renderea en `about:blank` sin baseURL → la stylesheet nunca se carga. Fix: helper `src/shared/pdf/inject-base-href.ts` con regex que prepend `<base href="${internalBaseUrl}/"/>` al primer `<head>`. Suite total local: **112 unit/component + 129 integration + 40 E2E = 281 tests verdes**.

## T-025 ✅ Streaming de generación IA en EditorView

Streaming de generación IA — feedback incremental durante la generación con Claude. Reemplaza el camino productivo de `generateInformeContentAction` (T-020, ahora `@deprecated`) por un **Route Handler POST `/api/informes/[id]/generate-stream`** que streamea Server-Sent Events. UX antes: 30-60s de spinner mudo; UX ahora: el markdown aparece a medida que Claude lo emite. Contrato SSE con 4 named events (`delta`, `usage`, `stop`, `error`, `done`) + parseo manual client-side con `TextDecoder` en `src/shared/ai/sse-client.ts` (~50 líneas isomorphic). Wrapper server-only `src/shared/ai/stream.ts` envuelve `client.messages.stream(params, { signal })` del SDK 0.95.1. Route handler reusa el patrón canónico T-023 (auth + getCurrentConsultora + RLS scope + UUID regex + audit log non-blocking via service-role), permission gate **creator OR owner**. `max_tokens=8192` y prompt caching `ephemeral` sobre el system block preservados. Audit log usa `action: 'informe_content_generated'`, `after_data` jsonb suma `stream: true`+`bytes_emitted`+`chunks_emitted`+`generation_ms`. `EditorView.tsx` refactorizado: state machine extendida `idle | generating-stream | generated | saving | saving_metadata`, `fetch('/api/...', { signal })` con `AbortController` + `getReader()` + `parseSseStream` async-iterable + buffer en `useRef` flush via `requestAnimationFrame`, botón "Cancelar generación" durante stream. **Cleanup `useEffect` al unmount** (abort + cancelAnimationFrame + clearTimeout). Tests: 12 unit `ai-sse-client.test.ts` + 7 integration `informes-generate-stream-auth.test.ts` + 5 integration `informes-generate-stream-flow.test.ts` + 2 E2E `informes-generate-stream.spec.ts`. Headers de la response: `Content-Type: text/event-stream; charset=utf-8` + `Cache-Control: no-cache, no-transform` + `X-Accel-Buffering: no`. Follow-ups: **T-025-FU1** `tech-debt` (remover `generateInformeContentAction` + migrar 9 tests al route handler, post-1-sprint estable de stream en prod), **T-025-FU2** `feature` (métricas custom Sentry tokens/sec + time-to-first-token). Suite total: **92 unit/component + 129 integration + 40 E2E = 261 tests verdes**.

## T-024 ✅ Imágenes/adjuntos + logo consultora

**Cierra Sprint 2** con 3 features bundleadas en 1 PR grande (~36 archivos). **(a) Logo consultora** que reemplaza el wordmark "ConsultoraDemo" hardcoded del header del PDF; **(b) imágenes en los 5 tipos** de informe, renderizadas como sección "Anexos visuales" en página separada del PDF (page-break-before always, page-break-inside avoid por figura, max-height 180mm, figcaption opcional); **(c) adjuntos no-imagen** (PDF/DOC/DOCX/XLS/XLSX) que aparecen en la UI con botón "Descargar" via signed URL on-demand + en el PDF como tabla "Anexos descargables" con filename + tipo + tamaño (sin embedding del binario, solo referencia).

**Storage**: Supabase Storage greenfield para el repo, 2 buckets privados (`consultora-logos` 2 MB cap PNG/JPG/WEBP, `informe-attachments` 10 MB cap imágenes + PDF/DOC/DOCX/XLS/XLSX) — SVG excluido (XSS vector) + HEIC/HEIF excluido v1 (requiere libheif).

Migrations: `20260513220316_informe_attachments.sql` (tabla con `consultora_id` denormalizado para RLS simples + 4 policies SELECT/INSERT/UPDATE/DELETE con gate creator OR owner + audit trigger `audit_informe_attachments()` con diff guard sobre `(filename, caption, position)` — NUNCA el binario), `20260513220317_consultora_logo.sql` (alter `consultoras` add `logo_storage_path text`), `20260513220318_storage_buckets.sql` (2 buckets + 7 storage policies usando helpers T-015 con cast `(storage.foldername(name))[1]::uuid`, defensa en profundidad sobre server actions).

**Sharp 0.33.5** promovido de `pnpm.onlyBuiltDependencies` a `dependencies` para usar el wrapper `processAttachmentImage()`/`processLogoImage()` que hace `rotate()` (honra EXIF Orientation) + `withMetadata({ exif: undefined, icc: undefined })` strip + resize a 2400px (attachment) / 600px (logo) con `withoutEnlargement` + re-encode al mismo formato. Module `src/shared/storage/`: `types.ts`, `validators.ts` (`validateAttachmentMime` + `validateAttachmentSize` + `sanitizeFilename` con NFKD + strip control chars + path separators + `magicBytesMatch` anti-MIME-spoof: PNG/JPG/WEBP/PDF/DOC/XLS CFB + DOCX/XLSX ZIP), `paths.ts`, `format.ts`, `sharp-pipeline.ts` (server-only wrapper), `attachments.ts` + `logo.ts`.

**Route handlers**: `POST /api/informes/[id]/attachments` multipart con `request.formData()` — bypasea cap 1 MB de server actions Next 16 — y flow: UUID regex → auth → `getCurrentConsultora` → `getInformeById` RLS → gate creator OR owner → quota check (max 20/informe) → parse FormData → validate MIME+size+magic bytes → si `kind='image'` corre sharp pipeline → `uploadAttachmentToStorage(adminClient)` → INSERT via cliente auth-ed → si INSERT falla, rollback storage delete; `POST /api/settings/consultora/logo` mismo flow para logo owner-only con cleanup `await` del path previo si existe.

**Server actions** en `src/app/(app)/informes/[id]/attachments/actions.ts`: `updateAttachmentCaptionAction` (image-only), `reorderInformeAttachmentsAction` (bulk UPDATE posiciones 0..N-1, verifica IDs + son imágenes), `deleteInformeAttachmentAction` (storage primero via service-role, después DB row). `removeConsultoraLogoAction` owner-only. Discriminated union return extendido con 4 codes nuevos: `PAYLOAD_TOO_LARGE`, `UNSUPPORTED_MIME`, `QUOTA_EXCEEDED`, `STORAGE_ERROR`, `MAGIC_BYTES_MISMATCH`.

**UI**: `AttachmentsSection.tsx` client con grid 2-3 cols responsive de thumbnails (Next `<Image unoptimized>` con signed URLs pre-generadas server-side TTL 1h), caption inline editable con `onBlur` → action, reorder con flechas ↑↓ (NO dnd-kit en v1), delete con `AlertDialog` confirmation, lista vertical de archivos con botón "Descargar". `ConsultoraSettingsView.tsx` client en `/settings/consultora` con preview del logo + botones "Reemplazar logo" / "Eliminar logo" (gate owner-only). Layout nuevo `src/app/(app)/settings/layout.tsx` placeholder. Sidebar nav suma item "Configuración".

**PDF integration** (PrintTemplate.tsx): header condicional con `<img class="pdf-brand-logo" max-height:48pt max-width:200pt object-fit:contain>` si hay `branding.logoSignedUrl`, fallback al wordmark con `consultora.name`; sección "Anexos visuales" después del body markdown con `page-break-before: always` y por figura `page-break-inside: avoid`; sección "Anexos descargables" con tabla simple.

**Hallazgo crítico durante smoke local PDF** (sin esto el feature NO funciona en producción): el CSP del `(print)/layout.tsx` heredado de T-023 era `img-src 'self' data:`, que bloquea silenciosamente las signed URLs cross-origin al host `*.supabase.co`. Fix: derivar el origin desde `NEXT_PUBLIC_SUPABASE_URL` y agregarlo dinámicamente al `img-src` server-side, con fallback `https:` si el env está malformado.

**T-024-FU2 entró al PR** (`docs/technical/06-deployment.md` sección "Chromium para PDF render"): documenta que dev local Windows/macOS necesita `CHROMIUM_PATH` apuntando a Chrome instalado (`puppeteer-core` no descarga binario propio); el Dockerfile alpine ya lo tiene seteado en `/usr/bin/chromium-browser` para prod.

**Tests**: 3 unit `attachment-validators` (38 tests) + `storage-paths` (15) + `human-format` (10); 3 integration `informe-attachments-rls` (13) + `informe-attachments-actions` (17) + `consultora-logo-actions` (9); 3 E2E nuevos: `informes-attachments.spec.ts` + `informes-attachments-permissions.spec.ts` + `consultora-logo.spec.ts`. Sidebar test actualizado `expect(items.length).toBe(8)` por sumar item "Configuración".

**Edge cases**: storage quota exceeded (409 QUOTA_EXCEEDED con cap 20/informe), orphan files si DB insert falla post-upload (rollback storage delete inline + cron FU1 para cleanup tardío), imagen EXIF-rotada (sharp `.rotate()` la endereza), delete informe → cascade DB pero NO storage objects, logo eliminado sin replace (fallback wordmark `consultora.name`), cross-tenant access intent (RLS doble: tabla + storage.objects policies).

**Cifras suite**: **175 unit/component** + **168 integration** + **44 E2E** + 1 skipped opt-in = **388 tests verdes** (+107 sobre baseline T-023-FU4).

**Follow-ups esperados**: T-024-FU1 (cron admin de cleanup huérfanos Storage > 24h), T-024-FU2 ✅ (Chromium dev local doc), T-024-FU3 (conversion HEIC/HEIF → JPG con libheif), T-024-FU4 (quotas globales por tenancy).
