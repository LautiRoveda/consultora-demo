# ConsultoraDemo

Plataforma SaaS para consultores de Higiene y Seguridad Laboral en Argentina, **potenciada por IA**, con foco en dos pilares: **generación de informes técnicos** y **calendario de vencimientos con alertas proactivas**. No es una suite EHS genérica — son dos cosas hechas mejor que la competencia.

## Si sos Claude Code o un agente IA

Leé esto en orden y vas a tener todo el contexto que necesitás:

1. **`docs/discovery/`** — el porqué del producto (negocio, mercado, personas, competencia, requerimientos).
2. **`docs/technical/`** — el cómo se construye (skills, principios, arquitectura, datos, estructura, roadmap).
3. **`docs/adr/`** — decisiones puntuales con contexto y consecuencias.

Los documentos están numerados por orden de lectura. **Ningún cambio al código se hace sin antes leer los documentos relevantes.** Si algo del código no respeta lo documentado, es deuda técnica y se abre ticket.

## TL;DR del producto

Un consultor argentino de HyS atiende entre 5 y 20 clientes (PYMEs e industrias). Su día se reparte 60-70% en papelería repetitiva (informes técnicos en Word, planillas Excel, agenda papel) y 30-40% en visitas reales. Pierde clientes por descuido (vencimientos olvidados) y se come multas por entregas de EPP fuera de plazo (Resolución SRT 299/11 obliga renovación cada 6 meses).

ConsultoraDemo resuelve dos cosas con IA:
1. **Genera el informe técnico** (ruido, iluminación, puesta a tierra, RGRL, carga de fuego) en 5 minutos en lugar de 2-4 horas. **Form estructurado por dominio** (CUIT, ART, áreas, turnos, modalidad operativa, …) + Claude Sonnet 4.6 con domain intelligence HyS argentina → output 80-90% completo que el profesional matriculado firma con edits menores (no placeholders `[A COMPLETAR]` por todos lados).
2. **Avisa antes de cada vencimiento** (protocolo anual, EPP a 6 meses, calibraciones, capacitaciones).

Pricing: USD 30/mes plan Pro. Trial de 7 días. Sin llamar a comercial.

## Stack

- **Frontend:** Next.js 16 con App Router + React 19 + TypeScript strict + Tailwind 4 + shadcn/ui.
- **Backend:** Next.js Route Handlers y Server Actions, en **VPS Hostinger Ubuntu 24.04** (EasyPanel + Docker, Node 22 alpine, `output: 'standalone'`). Hosting principal desde T-022.5 — ver ADR-0007. Imagen Docker ~600 MB con **Chromium-alpine + Puppeteer headless** para generación de PDFs server-side desde T-023.
- **DB / Auth / Storage:** Supabase (Postgres con RLS multi-tenant).
- **IA:** Anthropic Claude API (Sonnet 4.6 por default — ver ADR-0003).
- **Notificaciones:** Resend (email) + Telegram Bot API + push web.
- **Pagos:** Mercado Pago.
- **Tests:** Vitest + Playwright.
- **CI/CD:** GitHub Actions (235+ tests) + **Auto Deploy de EasyPanel** vía webhook GitHub tras cada merge a `main` (T-022.5-FU3 ✅ — habilitado en EasyPanel CE self-hosted). Vercel hot-backup pausado 4 semanas post-cutover.
- **Observabilidad:** Sentry (client + server + edge) + logs estructurados pino expuestos en EasyPanel UI.

Ver `docs/technical/00-skills-y-stack.md` para detalle y justificación.

## Principios no negociables

1. **Modularidad estricta.** 14 módulos independientes, conocidos solo por su API pública.
2. **Seguridad por defecto.** Auth en cada Server Action, validación Zod en cada borde, RLS en cada tabla, secrets en variables de entorno.
3. **Type safety end-to-end.** TypeScript strict, Zod en bordes, tipos generados desde schema SQL.
4. **Tests obligatorios para lógica de dominio.** > 70% cobertura. Pirámide 70/20/10.
5. **CI/CD desde el primer commit.** Branch `main` protegida, deploy automático.
6. **Observabilidad first-class.** Sentry, logs estructurados, métricas custom.
7. **Documentación viva.** ADRs, READMEs por módulo, CLAUDE.md sincronizado.
8. **Performance y accesibilidad como hard requirements.** Lighthouse > 90, WCAG AA.
9. **Costo bajo control.** Tracking de tokens IA por consultora, prompt caching, modelo según tarea.
10. **Simplicidad sobre cleverness.** Solución más simple que cumple, siempre.

Detalle en `docs/technical/01-principles.md`.

## Estructura del repo

```
consultora-demo/
├── docs/
│   ├── discovery/        ← negocio, mercado, personas, competencia, síntesis
│   ├── technical/        ← skills, principios, arquitectura, datos, estructura, roadmap
│   └── adr/              ← decisiones puntuales registradas
├── src/
│   ├── app/              ← rutas Next.js
│   ├── modules/          ← 14 módulos de negocio
│   ├── shared/           ← código compartido (UI base, supabase, ai, observability)
│   └── tests/            ← unit, integration, e2e
├── supabase/
│   ├── migrations/       ← SQL versionado
│   └── seed.sql
├── public/
├── .github/workflows/    ← CI/CD
└── ...
```

Detalle en `docs/technical/04-folder-structure.md`.

## 14 módulos

**Capa transversal:** Auth, Tenancy, Auditoría, Notificaciones.
**Capa coordinación:** Calendario.
**Capa negocio:** Informes, EPP, Checklists, Catálogo de Tareas, Accidentabilidad, Permisos de Trabajo, Documentos, Capacitaciones, Pagos.

Cada módulo en `src/modules/<nombre>/` con `actions.ts`, `queries.ts`, `schemas.ts`, `types.ts`, `index.ts`, `README.md` y, según necesidad, `components/` y subcarpetas específicas.

Detalle en `docs/technical/02-architecture.md`.

## Modelo de datos

Todas las tablas con `consultora_id` y RLS desde el día cero. UUIDs por defecto. Soft delete con `archived_at` donde corresponde. Audit log inmutable. Tipos TypeScript generados automáticamente desde el schema.

Schema completo en `docs/technical/03-data-model.md`.

## Roadmap

**Fase 1 (6-8 semanas) — MVP cobrable.** Auth, Tenancy, Auditoría, Notificaciones, Calendario, Informes (5 tipos), EPP con tracking y planilla Res 299/11, Checklists Lite, libro de incidentes, Pagos con Plan Pro USD 30. Trial 7 días.

**Fase 2 (4 semanas) — Plan Team.** Coordinación de equipo, asignación, aprobación, branding consultora.

**Fase 3 (5 semanas) — PWA offline + obra.** Permisos diarios, kit de jornada, captura cámara/GPS, sync diferido.

**Fase 4 (6 semanas) — Inteligencia.** Repositorio documental con OCR + RAG, capacitaciones automáticas, accidentabilidad con IA, Plan Enterprise con multi-establecimiento.

**Fase 5+** — Avanzadas (chat conversacional, visión computacional, integraciones, marketplace, internacionalización).

Roadmap detallado por tickets en `docs/technical/10-roadmap.md`.

## Estado actual

- **Discovery completo** ✅ (4 documentos en `docs/discovery/`).
- **T-026 (discovery) ✅** base para Sprint 3 Calendario + Notificaciones. 11 secciones (problema, casos de uso, modelo de datos, tipos, sistema de recordatorios, canales email/Telegram/Web Push, cron pg_cron + pg_net, wireframes ASCII, edge cases, 7 decisiones DA-01..DA-07, plan de tickets T-027..T-037). Ver [`docs/discovery/07-calendario-notificaciones.md`](docs/discovery/07-calendario-notificaciones.md). **DA-01..DA-07 ✅ resueltas 2026-05-14** — T-027 desbloqueado (migration `calendar_events` + `calendar_event_reminders` + RLS + audit trigger, próximo). Decisiones clave: HTML inline (DA-01), Web Push sin PWA en MVP (DA-02), settings per-user (DA-03), sin snooze (DA-04), auto-crear vencimiento al firmar como modal default + toggle silent opt-in (DA-05), `.ics` follow-up (DA-06), compartir con cliente fuera de scope (DA-07).
- **Diseño técnico completo** ✅ (6 documentos clave en `docs/technical/` incluyendo `06-deployment.md`).
- **ADRs iniciales** ✅ (template + ADR-0002 stack + ADR-0003 modelo Claude default + ADR-0004 branch protection diferida + ADR-0005 Vercel-GitHub auto-deploy [parcialmente reemplazado por ADR-0007] + ADR-0006 multi-tenant RLS strategy + **ADR-0007 VPS Hostinger + EasyPanel + Docker**).
- **Prototipo Fase 0** ✅ (`public/prototipo/index.html` estático, accesible vía rewrite `/prototipo`).
- **Sprint 0 — setup del repo** ✅ **COMPLETO (10/10)**
  - **T-001** ✅ Next.js 16 + TS strict + Tailwind 4 + shadcn/ui base.
  - **T-002** ✅ ESLint 9 (flat) + Prettier 3 + Husky 9 (commit-msg, pre-commit, pre-push).
  - **T-003** ✅ Vitest 3 (projects: unit, component) + Playwright (chromium).
  - **T-004** ✅ GitHub Actions CI (`.github/workflows/ci.yml`) + flow PR-based + hook pre-push contra push directo a `main` (branch protection server-side diferida, ver ADR-0004).
  - **T-005** ✅ Supabase CLI + proyecto remoto `consultora-demo` en sa-east-1 + migration de extensiones (uuid-ossp, pgcrypto, vector, pg_cron) aplicada al remote. Docker Desktop **no** instalado: trabajamos contra el remote.
  - **T-006** ✅ Cliente Supabase (server, browser, service-role) + helper proxy + validación de env con Zod en `src/env.ts` (server-only).
  - **T-007** ✅ Sentry (client + server + edge configs + `instrumentation.ts`) + logger pino con captura automática a Sentry en `error()`/`fatal()`. `/api/test-error` dev tool.
  - **T-008** ✅ Theme shadcn alineado al prototipo (indigo brand + 4 severity tokens) + 7 componentes base + `/styleguide` dev tool.
  - **T-009** ✅ Landing pública productiva (`/`) + `/login` UI (auth real T-012) + páginas legales `/terminos` y `/privacidad` con noindex + `robots.txt` + `sitemap.xml`. Lighthouse 97/100/100/100.
  - **T-010** ✅ Vercel deploy desde main con 9 env vars (Production + Preview) + `SENTRY_AUTH_TOKEN` activo (source maps automáticos) + ADR-0005 + runbook `docs/technical/06-deployment.md`. URL Vercel: <https://consultora-demo.vercel.app> (hot-backup post-T-022.5; **URL productiva actual: <https://consultora-demo.test-ia.cloud>**).
- **Sprint 1 — Auth + Tenancy + base multi-tenant** ✅ **COMPLETO (8/8)**
  - **T-011** ✅ Migration `tenancy.sql`: 3 tablas (`consultoras`, `consultora_members`, `audit_log`) + función `current_consultora_id()` + triggers + 5 RLS policies default-deny + ADR-0006.
  - **T-012** ✅ Signup flow productivo: `/signup` → `auth.signUp` + RPC `create_consultora_and_owner` (atómico, trial 7d, slug `unaccent`) → `/check-email` → email confirm → `/auth/callback?next=/login` → `/login?confirmed=1`.
  - **T-013** ✅ Login real (password) + magic link (botón secondary) + `/dashboard` stub (server-protected). `/auth/callback` con `?next=` allowlisted. Migration `dashboard_rls.sql` suma policy defensiva `consultoras_select_own_member`. `signOutAction` server-side.
  - **T-014** ✅ Password recovery completo + logout formalizado: `/recuperar-password` (form anti-enumeration) + `/cambiar-password` (server-protected) + `updatePasswordAction` con flujo `resetPasswordForEmail` → `/auth/callback?next=/cambiar-password` → `/dashboard?reset=ok`. Banner "Contraseña actualizada" en dashboard. Link "¿Olvidaste tu contraseña?" en LoginForm. 7 integration tests recovery + 6 E2E.
  - **T-015** ✅ RLS helpers SQL reusables: 4 funciones `stable security definer` en schema `public` (`is_member_of_consultora`, `is_owner_of_consultora`, `role_on_consultora`, `my_consultora_ids`). Policies pre-existentes refactorizadas (`consultoras_update_own_owner`, `consultoras_select_own_member`) — semántica idéntica, sin regresiones. 5 integration tests nuevos (13 → 18 RLS, 48/48 total). Migrations `20260511130757_rls_helpers.sql` + `20260511131522_rls_use_helpers.sql`. Dev tool `pnpm dev:smoke-rls-helpers`.
  - **T-016** ✅ Custom claim `consultora_id` en JWT via Supabase Auth Hook: `custom_access_token_hook()` inyecta `app_metadata.consultora_id` + `consultora_role` en cada token issue. `current_consultora_id()` refactor lee del claim. Fast-path en los 4 RLS helpers de T-015 (claim primero, fallback a `consultora_members`). Refresh explícito post-signin + post-callback PKCE. Validado E2E en prod: JWT real trae los claims.
  - **T-017** ✅ Layout autenticado con route group `(app)`: server-protected layout valida sesión + carga consultora via helper `getCurrentConsultora` (decodifica JWT claim para fast-path + fallback a `consultora_members`). App shell con sidebar (desktop fija + mobile Sheet), nav items con `usePathname`, user menu (DropdownMenu con cambiar contraseña + logout via `useTransition`). 4 shadcn components nuevos (sheet, dropdown-menu, avatar, tooltip). Dashboard simplificado con cards "Próximamente" por feature. Migración: `src/app/dashboard/*` → `src/app/(app)/dashboard/*`; `signOutAction` → `src/shared/auth/actions.ts`. Alert `?error=no_consultora` en LoginForm para edge case (user autenticado sin membership).
  - **T-018** ✅ E2E auth flow con Playwright: 5 tests con sesión real en `src/tests/e2e/auth-flows.spec.ts` (layout protection sin sesión + `no_consultora` alert + login happy path + logout + recovery completo 7 pasos). Helpers reusables en `src/tests/e2e/helpers/` (`createTestUserWithConsultora`, `deleteTestUser`, `generateRecoveryLinkUrl`, `loginViaUI`, `logoutViaUI`) que bypasean email rate limit via `admin.createUser({email_confirm:true})` + `admin.generateLink`. Cierra drift de placeholders Supabase en `ci.yml` → GitHub Secrets reales. Bump Node 20 → 22 LTS en CI (WebSocket nativo requerido por `@supabase/realtime-js`). Suite total: **39 unit/component + 57 integration + 28 E2E = 124 tests verdes** corriendo en CI sin opt-in flags.

- **Sprint 2 — Informes** ✅ **COMPLETO (8/8)**
  - **T-019** ✅ Módulo Informes MVP — primer ticket del módulo de negocio, sienta patrón forward para Clientes/Empleados/EPP/Calendario. Migration `20260511232802_informes.sql`: tabla `public.informes` (`tipo` text+check con 5 valores · `titulo` · `contenido` nullable · `status` `draft|published|archived` · `created_by`) + 2 indexes + 3 RLS policies (SELECT/INSERT/UPDATE con WITH CHECK) usando helpers de T-015 + sin policy DELETE (default-deny para authenticated; UI usa `status='archived'` como soft-archive) + función `audit_informes()` (security definer) con triggers AFTER INSERT/UPDATE/DELETE que escriben a `audit_log` con diff jsonb en `before_data`/`after_data`, cumpliendo la promesa de `tenancy.sql:237-238`. UI MVP bajo route group `(app)`: `/informes` (lista + empty state), `/informes/nuevo` (RHF + zodResolver + shadcn Select), `/informes/[id]` (placeholder de contenido — editor llega en T-020). `createInformeAction` con discriminated union. Sidebar item Informes pasa de `soon` → `live`. Tests: 21 integration RLS/audit/check + 3 integration actions con session cookie mockeado + 2 E2E happy path + 1 nuevo unit en `AppSidebarNav.test.tsx`. Suite total: **40 unit/component + 81 integration + 30 E2E = 151 tests verdes**.
  - **T-020** ✅ Editor de contenido + generación con Claude API — primer hit productivo a Anthropic. Anthropic SDK 0.95.1 (singleton server-only en `src/shared/ai/anthropic.ts`, modelo `claude-sonnet-4-6` por ADR-0003). 5 system prompts hardcoded en `src/shared/ai/prompts/` por tipo de informe (relevamiento/capacitacion/rgrl/accidente/otros), cada uno con header común (rol HyS AR, regla PII Ley 25.326, anti-invención de cuantitativos/normativa) + secciones específicas. `generateInformeContentAction` con discriminated union de 9 codes (INVALID_INPUT/UNAUTHENTICATED/NO_CONSULTORA/FORBIDDEN/NOT_FOUND/RATE_LIMITED/CONTENT_FILTER/TIMEOUT/INTERNAL_ERROR), prompt caching `ephemeral` sobre system block, `max_tokens: 4096` por Vercel Hobby 10s timeout (#26 sube a 8192 con Pro tier), logger nunca registra prompt ni response — solo metadata. `updateInformeContentAction` con permission gate defensivo + RLS WITH CHECK del lado DB. Migration `20260512003301_audit_informes_include_contenido.sql` extiende `audit_informes()`: diff guard ahora incluye `contenido`, payload jsonb agrega `contenido_size` + `contenido_preview` (500 chars + `...`). UI `/informes/[id]/editar` con `EditorView` client (RHF + zodResolver, state machine idle/generating/generated/saving, textarea userPrompt con cap 2000 chars + contador, botón "Generar con IA", preview markdown live), `MarkdownPreview` server reutilizable (react-markdown + remark-gfm + rehype-sanitize). `/informes/[id]` ahora renderiza markdown real con botón Editar condicional (creator OR owner). shadcn `textarea` instalado. Tests: 9 integration con `vi.mock('@/shared/ai/anthropic')` (preserva error classes reales del SDK) + 2 E2E (save flow + permission gate UI) + 1 unit nuevo en `env.test.ts`. Follow-up `#26` T-020-FU1 (`tech-debt`, `T-020-followup`). Suite total: **41 unit/component + 90 integration + 32 E2E = 163 tests verdes**.
  - **T-021** ✅ Templates parametrizados RGRL — primer **form-driven generation con domain intelligence**. Migration `20260512015104_informe_metadata.sql`: tabla `public.informe_metadata` (1:1 con `informes` via PK=FK `on delete cascade` · `data jsonb`) + 3 RLS policies (SELECT/INSERT/UPDATE) con `EXISTS`-subquery contra `informes` y helpers de T-015 (gate creator OR owner) + función `audit_informe_metadata()` (security definer) con guard `pg_column_size(data) <= 4 KB` y fallback `{_truncated: true}` sin `_field_count` innecesario. Módulo shared en `src/shared/templates/rgrl/`: schema Zod de 14 fields (`razon_social`, `cuit`, `domicilio`, `localidad`, `provincia` enum 24 valores, `actividad_principal`, `codigo_ciiu` opcional, `cantidad_empleados`, `distribucion_turno` enum, `modalidad_operativa` enum, `art_contratada`, `servicio_hys_modalidad` enum, `areas_relevadas` array min 1 max 20, `riesgos_pre_detectados` opcional, `fecha_relevamiento`) + 5 constantes (PROVINCIAS_AR, DISTRIBUCION_TURNO, MODALIDAD_OPERATIVA, SERVICIO_HYS_MODALIDAD, AREAS_RELEVADAS_PRESETS) + helpers `normalizeCuit` / `normalizeRgrlMetadata` + `renderRgrlMetadataAsPromptContext` con sanitización anti-prompt-injection (escape backticks + blockquote para riesgos + footer re-anclaje) + `RgrlMetadataForm` (client RHF, 5 secciones responsive `md:grid-cols-2`, checkbox group iterando presets + textarea "Otras áreas" con dedup case-insensitive cap 20, `<Input type="date">` nativo, `normalizeCuit` onBlur) + `RgrlMetadataSummary` (client con Collapsible "Ver datos completos" + `useMediaQuery` SSR-safe con `useSyncExternalStore`). `generateInformeContentAction` extendido: si `tipo='rgrl'` y hay metadata, prepende al **user message** (system block intacto → prompt caching ephemeral preservado), fallback no bloqueante a comportamiento T-020 si metadata no parsea (drift). `updateInformeMetadataAction` nueva con UPSERT por `informe_id`. `createInformeAction` acepta `metadata` opcional, no bloqueante si persist falla. UI `/informes/nuevo` rewritten a wizard 2-step (`AlertDialog` "Crear sin datos" con label "Crear vacío"), `/editar` suma panel Collapsible arriba con form RGRL + acción separada "Guardar datos", `/[id]` renderiza summary arriba del markdown cuando hay metadata. 3 shadcn components nuevos (`alert-dialog`, `collapsible`, `checkbox`) + 1 hook `useMediaQuery` en `src/shared/lib/`. Tests: 5 integration `informes-metadata-actions` (INVALID_INPUT/UNAUTHENTICATED/NOT_FOUND/FORBIDDEN/UPSERT + audit) + 5 integration `informes-metadata-rls` (SELECT cross-tenant denied + SELECT permitido owner + INSERT denied member-non-owner + UPDATE denied cross-tenant + cascade DELETE) + 4 nuevos en `informes-content-actions.test.ts` (inyecta context + shape end-to-end + combina notes + fallback schema-drift) + 2 E2E `informes-rgrl-template.spec.ts` (wizard happy path + permission gate UI sobre summary). Smoke productivo: output del LLM con todos los valores del form inyectados, placeholders `[A COMPLETAR]` solo en campos no provistos, domain knowledge correcto (Decreto 1338/96, 351/79, WBGT, LOTO, audiometrías), Claude detectó inconsistencia provincia ↔ localidad y la corrigió con nota al firmante. Suite total: **41 unit/component + 104 integration + 34 E2E = 179 tests verdes**.
  - **T-022** ✅ Templates parametrizados para los 4 tipos restantes — **completa el módulo Informes con form-driven generation cross-tipo**. 4 schemas Zod nuevos en `src/shared/templates/<tipo>/schema.ts`: `capacitacion` (10 fields: 3 cliente + fecha + modalidad enum + duracion_horas decimal + tema_principal + capacitador_nombre + capacitador_matricula opt + cantidad_asistentes + contenidos_resumen opt), `relevamiento` (8 fields: 5 cliente+sitio + fecha + areas_relevadas array + agentes_a_relevar enum array de 10 valores HyS + equipos_medicion opt), `accidente` (12 fields: 3 cliente + fecha + hora HH:MM + lugar + puesto + tipo_lesion array Anexo I Res. SRT 1604/07 + partes_cuerpo array + gravedad enum + dias_baja opt + testigos boolean + descripcion_inicial min 10 max 4000), `otros` (4 fields wildcard: razon_social + cuit + tema_informe + objetivos opt). Módulo `common/` nuevo con `commonClientFields()`/`commonClientFieldsWithSite()` factories + `sanitizeField`+`renderAsBlockquote` + `normalizeCuit` + `PROVINCIAS_AR`+`provinciaName` + `AREAS_RELEVADAS_PRESETS` + `fechaIsoField`+`HORA_HHMM_REGEX` + `summary-ui.tsx` (`Item`+`StatusBadge`+`formatFecha`). 4 renders markdown en `<tipo>/render.ts` con footer de re-anclaje específico por tipo (accidente: NO inventar causa raíz ni testigos; relevamiento: umbrales SRT Decreto 351/79 Anexo V + Res. 295/03; capacitación: no inventar listado nominal; otros: adapta a tema+objetivos sin estructura impuesta). 4 `<Tipo>MetadataForm` client + 4 `<Tipo>MetadataSummary` client siguiendo patrón canónico T-021 (grid 2 cols desktop, Separator+h3 por sección, RHF FormField, Collapsible "Ver datos completos"). **Registry pattern split**: `registry/server.ts` (schemas+renders+normalize, importable desde Server Actions sin arrastrar JSX) + `registry/client.tsx` (defaults+FormComponent+SummaryComponent, bundle visual). `updateInformeMetadataInputSchema` → **discriminated union** `z.discriminatedUnion('tipo', [...])` con 5 variantes; el action verifica `input.tipo === informe.tipo` server-side. Wizard `/informes/nuevo` refactor con `useFormsByTipo()` hook que instancia 5 `useForm` hardcoded (React Rules of Hooks no permite Object.fromEntries+map) — volver al mismo tipo preserva values (UX preferida). `EditorView` refactor: `FormComponent` dinámico del registry; `useForm<FieldValues>` con `SCHEMA_BY_TIPO[tipo]` casteado a `ZodType<FieldValues, FieldValues>`. `/informes/[id]/page.tsx` refactor: `SummaryComponent` dinámico. `getInformeMetadata(supabase, id, tipo)` genérica con return type discriminado `{ tipo, data: unknown } | null`. **RGRL adopta `commonClientFieldsWithSite`+`fechaIsoField`** sin breaking (jsonb keys idénticas, smoke check verde contra 53 filas reales en remote). Tests: 4 unit `templates-<tipo>-schema.test.ts` (~7 tests c/u con happy path + validación bounds + normalize + render sanitize) + 5 nuevos integration en `informes-metadata-actions` via `describe.each(tipoFixtures)` (4 happy path UPSERT por tipo + 1 mismatch tipo) + 2 E2E `informes-capacitacion-template.spec.ts` (wizard happy path + save metadata). Ajuste de `informes.spec.ts` (T-019) al wizard 2-step (los 5 tipos ramifican ahora, no hay "quick path"). Smoke productivo en preview con los 4 tipos nuevos: **outputs truncated por `max_tokens=4096`** (capacitación corta en Anexo D, accidente en sección 7 hallazgos, relevamiento en tabla 4.3 ergonomía, otros en sección 5.4.2). Calidad excelente hasta el cut — follow-up T-022-FU1 (`#36`, cierra automáticamente con T-022.5 migración VPS). Follow-up T-022-FU2 (`#37`, flaky retry pre-existente T-020). Suite total: **68 unit/component + 109 integration + 36 E2E = 213 tests verdes**.
  - **T-022.5** ✅ Migración deployment Vercel → **VPS Hostinger + EasyPanel + Docker** — destraba el cap `max_tokens` (sin timeout de plataforma) y baja USD 20/mes vs Vercel Pro. PR `#39` merge SHA `f61aec2`. ADR-0007 con config literal del Service para EasyPanel UI. `next.config.ts` `output: 'standalone'` (imagen Docker ~350 MB vs ~1.2 GB). `Dockerfile` multi-stage Node 22 alpine + corepack `pnpm@11.0.9` + user no-root, **deps stage usa `pnpm install --frozen-lockfile --ignore-scripts` + `pnpm rebuild sharp esbuild`** (workaround dual: bypass `ERR_PNPM_IGNORED_BUILDS` de pnpm 11 strict mode + husky `prepare` postinstall fail por `.git` excluido en `.dockerignore`). `actions.ts` sube `max_tokens` 4096 → 8192 (cap teórico Sonnet 4.6 = 64 k). Deploy operacional: click manual "Implementar" en EasyPanel UI tras cada merge fue el workflow inicial (v2.30.0 self-hosted, probamos 2 approaches automáticos antes de aceptar manual). **T-022.5-FU3 ✅ Auto Deploy de EasyPanel habilitado posteriormente** — push a `main` dispara deploy automático vía webhook GitHub sin intervención. Vercel queda como **hot backup 4 semanas** con auto-deploy pausado, decommission programado +28 días (tracking T-022.5-FU2). Cierra issues `#26` (T-020-FU1) + `#36` (T-022-FU1). Suite total mantiene **213 tests verdes** (cambios sin impacto de runtime); CI ya no tiene job `deploy` (lo dispara EasyPanel del lado del VPS). Smoke productivo end-to-end ✅ con los 5 tipos generando outputs completos sin truncar. **URL productiva: <https://consultora-demo.test-ia.cloud>**.
  - **T-023** ✅ Export PDF de informes con Puppeteer headless — primer feature de "output" del módulo Informes. Endpoint `GET /api/informes/[id]/pdf` con auth + permission gate (cualquier member de la consultora exporta — mismo gate que SELECT, exportar es lectura), validación UUID del path param, gate de contenido vacío (422 `EMPTY_CONTENT`), audit log `informe_exported_pdf` via service-role (non-blocking — el user ya pagó la generación, perder el log es menos malo que perder el PDF), headers RFC 6266 (`filename*` UTF-8 + `filename` ascii) + Cache-Control private + X-Robots-Tag noindex. Módulo `src/shared/pdf/`: `browser-pool.ts` con singleton lazy de Chromium (idle timeout 5min, HMR-safe via `globalThis` para token efímero `crypto.randomBytes(32)`, SIGTERM/SIGINT cleanup hooks, flags `--single-process` + `--no-sandbox` + `--disable-dev-shm-usage` acordes con alpine + user no-root); `render.ts` con `htmlToPdf()` wrapper (timeouts en cascada: setContent 10s + page.pdf 15s + AbortController 20s hard cap, `PdfRenderTimeoutError` mapeable a HTTP 504); `filename.ts` con `slugifyTitulo()` NFKD + drop diacritics + cut en boundary de palabra y `buildPdfFilename()` formato `informe-<tipo>-<slug>-YYYY-MM-DD.pdf`. Vista print en route group `(print)/` **sibling de `(app)/`** — única forma Next.js de bypasear AppShell (nested layouts componen, no reemplazan); layout valida `x-internal-pdf-render` token efímero, sin match → `notFound()`. `PrintTemplate.tsx` server component reusa `MarkdownPreview` con CSS print inline. `DownloadPdfButton.tsx` client con state machine `idle | downloading`, Blob + `URL.createObjectURL` + anchor.click trigger, parseo Content-Disposition `filename*` UTF-8 con fallback ascii, toasts sonner por código de error (UNAUTHENTICATED con CTA Iniciar sesión, EMPTY_CONTENT warning, RENDER_TIMEOUT, NO_CONSULTORA, genérico); disabled + Tooltip "Generá contenido antes de descargar" cuando informe vacío. **Dockerfile** stage runner: `apk add chromium nss freetype harfbuzz ttf-freefont ttf-dejavu font-noto` + env vars Puppeteer apuntan a `/usr/bin/chromium-browser`. Imagen Docker **~350 MB → ~600 MB** (Chromium + fonts). `next.config.ts` suma `serverExternalPackages: ['puppeteer-core']`. `package.json` suma `puppeteer-core ^23.11.1` (no `puppeteer` full — Chromium se instala via apk en el container, evita descarga npm de 170MB). Tests: 12 unit `pdf-filename.test.ts` (slugify edge cases + buildPdfFilename format) + 6 integration `pdf-route-auth.test.ts` (auth gates + cross-tenant 404 + empty content 422 + happy path) + 2 integration `pdf-route-audit.test.ts` (audit log fila correcta + no-fila en render fail) + 2 E2E `informes-pdf-export.spec.ts` (happy path con download + magic bytes `%PDF-` + filename regex + informe vacío con botón disabled + tooltip). PR `#43` merge SHA `2d595b2c`. **Polish post-deploy** (PR `#48` merge SHA `52a6004`): márgenes generosos para impresión física A4 (22→25mm top, 18→22mm sides, 24→38mm bottom) + footer `font-size: 6.5pt` + `max-height: 15mm` + `overflow: hidden` + bloque `@media print` con page-break controls (`h1/h2/h3 page-break-after: avoid`, `ul/ol page-break-inside: auto`, `li orphans/widows: 2`, `ul/ol > li:first/last-child` arrastra siguiente, `section/table/pre/blockquote page-break-inside: avoid`, `thead display: table-header-group` para repetir header de tabla en page break) + `body padding-bottom: 10mm` buffer extra. `.pdf-root` padding pasa a 0 (eliminé duplicación con margen Puppeteer/@page que causaba asimetría página 1 vs resto). Smoke productivo validado en `consultora-demo.test-ia.cloud` con RGRL Smoke de 15 páginas: **NINGUNA página con overlap body↔footer + márgenes cómodos para impresión A4**. **Follow-ups abiertos**: `#44` T-023-FU1 (mem_limit 1g en EasyPanel — mitigación cotenant interference con `agendalo` + `aruba`, activar al primer signo de presión RAM), `#45` T-023-FU2 ✅ (`resolveInternalBaseUrl` extraído a `src/shared/lib/resolve-internal-base-url.ts`; en `NODE_ENV=production` devuelve loopback IPv4 `http://127.0.0.1:${PORT ?? '3000'}` por default — la env var `INTERNAL_BASE_URL=http://localhost:80` en EasyPanel **ya no es necesaria; pueden removerla** post-deploy). +7 unit tests `pdf-resolve-internal-base-url.test.ts` (override prevalece sobre NODE_ENV, trailing slash strippeado, string vacío cae al next case, prod default + PORT custom, dev path por request.url origin, throw fail-fast en edge). **T-023-FU4 ✅ refactor + fix CSS load para PDF print** — bug visual de página 1 resuelto en 2 capas. **(a) Server/client split del SummaryComponent**: el `<CollapsibleTrigger>` shadcn + `<ChevronDown>` lucide quedaban inertes en cada PDF (los 5 tipos) porque PrintTemplate consumía `<Tipo>MetadataSummary` client (Collapsible+useMediaQuery+useState) y Puppeteer no hidrata → trigger+chevron render con state SSR inicial `effectiveOpen=false`. Refactor con 5 sibling Server Components nuevos `src/shared/templates/<tipo>/<Tipo>MetadataSummaryContent.tsx` (grid `grid-cols-2` plano con todos los campos, sin Collapsible, h2 propio por tipo: "Datos del relevamiento"/"...capacitación"/"...accidente"/"...informe"). Nuevo `src/shared/templates/registry/print.tsx` con `TEMPLATE_PRINT_REGISTRY` consumido por PrintTemplate. Web `<Tipo>MetadataSummary` (client, compact+Collapsible expand) **queda intacto** — blast radius 0 en `/informes/[id]`. Hardcode `Datos del establecimiento` eliminado de PrintTemplate (era RGRL-específico). **(b) Fix `<base href>` para que Tailwind cargue en PDF**: post (a), smoke visual reveló que el grid colapsaba a 1-col y el StatusBadge no rendía como pill. Diagnóstico via dump temporal del HTML pre-`page.pdf()`: las clases Tailwind estaban literales en el DOM y el CSS bundle compilado tenía las rules — pero el `<link rel="stylesheet" href="/_next/static/chunks/...">` usa URL relativa y Puppeteer `page.setContent(html)` renderea en `about:blank` sin baseURL → la stylesheet nunca se carga, todas las clases Tailwind son no-op. Solo el `<style>` inline del PrintTemplate (que targetea `body`/`h1`/`p`/`table`/etc.) aplicaba, dando la ilusión de "Tailwind parcial". Fix: helper `src/shared/pdf/inject-base-href.ts` con regex que prepend `<base href="${internalBaseUrl}/"/>` al primer `<head>` del HTML antes de pasar a `htmlToPdf()` en el route handler PDF. Reusa el `baseUrl` ya computed via `resolveInternalBaseUrl(request)` (T-023-FU2). Edge case: HTML sin `<head>` → retorna sin cambios + `logger.warn` (defensivo: PDF con clases default > error fatal). +3 unit tests `pdf-inject-base-href.test.ts` (happy path con `<head>` populado, trailing slash strip, html sin head con warn assertion). **(c) Import `globals.css` en `(print)/layout.tsx`** que agregué en una iteración intermedia ya no es estrictamente necesario tras el fix `<base href>` (el outer root layout ya importa globals.css y ahora con base href esa stylesheet resuelve), pero lo dejé porque hace explícita la dependencia print → globals.css y es defensivo si en un follow-up T-023-FU5 se refactoriza el (print) a root layout independiente. **Bug colateral del HTML inválido** (doble `<html>/<body>` por conflict entre root layout y print layout) abierto como **T-023-FU5 follow-up (issue `#53`, `tech-debt`)** — no urgente, Chrome lo tolera vía tag soup parsing. Sin migration, sin deps nuevas, sin CSS hacks `@media print`. Tests: +10 component `src/tests/unit/<Tipo>MetadataSummaryContent.test.tsx` (2 por tipo, happy path + missing-optionals) + 3 unit `pdf-inject-base-href.test.ts`. Cierra issues `#26` (T-020-FU1 max_tokens vía VPS), `#46` (T-023-FU3 footer overlap pág 1, superseded por FU4). Suite total local: **112 unit/component + 129 integration + 40 E2E = 281 tests verdes** (+13 sobre baseline T-025).
  - **T-025** ✅ Streaming de generación IA en EditorView — feedback incremental durante la generación con Claude. Reemplaza el camino productivo de `generateInformeContentAction` (T-020, ahora `@deprecated`) por un **Route Handler POST `/api/informes/[id]/generate-stream`** que streamea Server-Sent Events. UX antes: 30-60s de spinner mudo; UX ahora: el markdown aparece a medida que Claude lo emite. Contrato SSE con 4 named events (`delta`, `usage`, `stop`, `error`, `done`) + parseo manual client-side con `TextDecoder` en `src/shared/ai/sse-client.ts` (~50 líneas isomorphic, sin dep nueva — `eventsource-parser` sumaría 3KB para lo mismo). Wrapper server-only `src/shared/ai/stream.ts` envuelve `client.messages.stream(params, { signal })` del SDK 0.95.1: traduce eventos crudos del SDK al contrato SSE, mapea errores del SDK al discriminated union de T-020 (los 9 codes existentes + nuevo `STREAM_ABORTED`), callbacks `onComplete` (post-`message_stop` con stop_reason válido — escribe audit log) y `onAbort` (mid-stream cancel — pino-only, no audit log). Route handler reusa el patrón canónico T-023 (auth + getCurrentConsultora + RLS scope + UUID regex + audit log non-blocking via service-role), permission gate **creator OR owner** (mismo gate que update — generar requiere ser quien puede guardar). `max_tokens=8192` y prompt caching `ephemeral` sobre el system block preservados, registry T-022 (`getServerTemplate(tipo)`) inyecta metadata RGRL/capacitación/etc al user message exactamente igual que la action. Audit log usa `action: 'informe_content_generated'` (mismo string que T-020 — dashboard de tokens no se entera del cambio), `after_data` jsonb suma `stream: true`+`bytes_emitted`+`chunks_emitted`+`generation_ms` para telemetría. `EditorView.tsx` refactorizado: state machine extendida `idle | generating-stream | generated | saving | saving_metadata`, `fetch('/api/...', { signal })` con `AbortController` + `getReader()` + `parseSseStream` async-iterable + buffer en `useRef` flush via `requestAnimationFrame` (fallback `setTimeout(250ms)` para tab en background), botón "Cancelar generación" durante stream que dispara `abort()`. **Cleanup `useEffect` al unmount** (abort + cancelAnimationFrame + clearTimeout) — sin esto, navegar mid-stream filtra el fetch y el server sigue gastando tokens hasta el `message_stop`. Toasts sonner reusan mapping de codes T-020; `STREAM_ABORTED` es silent (fue acción intencional del user). Save strategy intacto: cliente acumula chunks → al `done` `form.setValue('content', buffer, { shouldDirty: true })` + state `generated` → user clickea "Guardar cambios" → `updateInformeContentAction` existente. Acción `generateInformeContentAction` marcada `@deprecated since T-025` en JSDoc; los 9 tests integration de T-020 siguen ejercitándola hasta T-025-FU1 (1 sprint estable en prod). Tests: 12 unit `ai-sse-client.test.ts` (eventos bien formados, fragmentación inter-chunk + intra-chunk, multi-line `data:`, comments `:heartbeat`, CRLF, unicode multi-byte partido entre chunks, bloque sin terminator descartado, espacio inicial post-`:` strippeado) + 7 integration `informes-generate-stream-auth.test.ts` (UUID malformado, body inválido > 2000 + no-JSON, sin sesión, sin consultora, cross-tenant 404 via RLS, member non-creator non-owner FORBIDDEN) + 5 integration `informes-generate-stream-flow.test.ts` con `makeStreamMock` async-iterable que emite `RawMessageStreamEvent`s (happy path delta+stop+usage+done en orden + audit_log row con `stream=true`, `RateLimitError` propaga `RATE_LIMITED`, `stop_reason='refusal'` propaga `CONTENT_FILTER`, metadata RGRL inyectada en user message, abort mid-stream con `pauseEachMs:10` emite `STREAM_ABORTED` + 0 rows en audit_log) + 2 E2E `informes-generate-stream.spec.ts` con `page.route` mock SSE pre-armado (happy path: stream visible → save persiste; cancel: route nunca fulfilla → user clickea Cancelar → state vuelve a idle, contenido previo preservado). **Sin migrations** (este ticket no toca DB). **Sin deps nuevas**. Headers de la response: `Content-Type: text/event-stream; charset=utf-8` + `Cache-Control: no-cache, no-transform` + `X-Accel-Buffering: no` (defensivo contra proxies con buffering). Follow-ups abiertos: **T-025-FU1** `tech-debt` (remover `generateInformeContentAction` + migrar 9 tests al route handler, post-1-sprint estable de stream en prod), **T-025-FU2** `feature` (métricas custom Sentry tokens/sec + time-to-first-token — útil cuando lleguen capacitaciones IA / accidentabilidad). Suite total: **92 unit/component + 129 integration + 40 E2E = 261 tests verdes**.
  - **T-024** ✅ Imágenes / adjuntos en informes + logo consultora — **cierra Sprint 2** con 3 features bundleadas en 1 PR grande (~36 archivos). **(a) Logo consultora** que reemplaza el wordmark "ConsultoraDemo" hardcoded del header del PDF; **(b) imágenes en los 5 tipos** de informe, renderizadas como sección "Anexos visuales" en página separada del PDF (page-break-before always, page-break-inside avoid por figura, max-height 180mm, figcaption opcional); **(c) adjuntos no-imagen** (PDF/DOC/DOCX/XLS/XLSX) que aparecen en la UI con botón "Descargar" via signed URL on-demand + en el PDF como tabla "Anexos descargables" con filename + tipo + tamaño (sin embedding del binario, solo referencia). **Storage**: Supabase Storage greenfield para el repo, 2 buckets privados (`consultora-logos` 2 MB cap PNG/JPG/WEBP, `informe-attachments` 10 MB cap imágenes + PDF/DOC/DOCX/XLS/XLSX) — SVG excluido (XSS vector) + HEIC/HEIF excluido v1 (requiere libheif). Migrations: `20260513220316_informe_attachments.sql` (tabla con `consultora_id` denormalizado para RLS simples + 4 policies SELECT/INSERT/UPDATE/DELETE con gate creator OR owner + audit trigger `audit_informe_attachments()` con diff guard sobre `(filename, caption, position)` — NUNCA el binario), `20260513220317_consultora_logo.sql` (alter `consultoras` add `logo_storage_path text`), `20260513220318_storage_buckets.sql` (2 buckets + 7 storage policies usando helpers T-015 `is_member_of_consultora` + `is_owner_of_consultora` con cast `(storage.foldername(name))[1]::uuid`, defensa en profundidad sobre server actions). **Sharp 0.33.5** promovido de `pnpm.onlyBuiltDependencies` a `dependencies` para usar el wrapper `processAttachmentImage()`/`processLogoImage()` que hace `rotate()` (honra EXIF Orientation — foto de iPhone con orientation=6 sale rotada en cualquier visor que no respete EXIF, incluyendo Puppeteer) + `withMetadata({ exif: undefined, icc: undefined })` strip + resize a 2400px (attachment) / 600px (logo) con `withoutEnlargement` + re-encode al mismo formato (evita conversion no consentida + previene smuggling). Module `src/shared/storage/`: `types.ts` (constantes MIME whitelist + caps + bucket names + TTLs `SIGNED_URL_TTL_PDF_RENDER_SEC=300` / `UI_SEC=3600` / `DOWNLOAD_SEC=60`), `validators.ts` (`validateAttachmentMime` + `validateAttachmentSize` + `sanitizeFilename` con NFKD + strip control chars `\x00-\x1f\x7f` + path separators + `magicBytesMatch` anti-MIME-spoof: PNG `89 50 4E 47 0D 0A 1A 0A`, JPG `FF D8 FF`, WEBP `RIFF....WEBP`, PDF `%PDF`, DOC/XLS CFB `D0 CF 11 E0 A1 B1 1A E1`, DOCX/XLSX ZIP `PK\x03\x04`+variantes — falla cerrado para MIMEs desconocidos), `paths.ts` (build + parse paths `<consultora>/<informe>/<uuid>.<ext>` y `<consultora>/logo-<ts>.<ext>` con asserts UUID), `format.ts` (`humanBytes` con 1 decimal salvo bytes + `humanMime` con labels PNG/JPG/PDF/etc), `sharp-pipeline.ts` (server-only wrapper), `attachments.ts` + `logo.ts` (helpers de `supabase.storage` para upload/delete/signed URLs). **Route handlers**: `POST /api/informes/[id]/attachments` multipart con `request.formData()` — bypasea cap 1 MB de server actions Next 16 — y flow: UUID regex → auth → `getCurrentConsultora` → `getInformeById` RLS → gate creator OR owner → quota check (max 20/informe) → parse FormData → `validateAttachmentMime` + `validateAttachmentSize` + `kindForMime` → `arrayBuffer()` → `magicBytesMatch` → si `kind='image'` corre sharp pipeline (rotate+strip+resize) → `buildAttachmentPath` → `uploadAttachmentToStorage(adminClient)` (service-role bypassa storage policies, gate ya validado server-side) → INSERT via cliente auth-ed (RLS WITH CHECK + audit trigger captura `auth.uid()`) → si INSERT falla, rollback storage delete → `revalidatePath` + return 201; `POST /api/settings/consultora/logo` mismo flow para logo owner-only con cleanup `await` (no `void`) del path previo si existe — descubierto que `void deleteLogoFromStorage(...).catch(...)` no espera la propagación a CDN en tests, ahora `await` con `removed: number` retornado para distinguir "borró 1" vs "no estaba". **Server actions** en `src/app/(app)/informes/[id]/attachments/actions.ts` para operaciones sobre filas existentes (body chico, no necesitan route handler): `updateAttachmentCaptionAction` (image-only — files no aceptan caption), `reorderInformeAttachmentsAction` (recibe `orderedIds[]`, bulk UPDATE posiciones 0..N-1, verifica que TODOS los IDs pertenecen al informe + son imágenes), `deleteInformeAttachmentAction` (storage primero via service-role, después DB row — si storage falla no toca DB, si DB falla post-storage queda huérfano para cron FU1). `removeConsultoraLogoAction` owner-only en `src/app/(app)/settings/consultora/actions.ts`. Discriminated union return extendido con 4 codes nuevos: `PAYLOAD_TOO_LARGE`, `UNSUPPORTED_MIME`, `QUOTA_EXCEEDED`, `STORAGE_ERROR`, `MAGIC_BYTES_MISMATCH` (sobre los 9 de T-020). **UI**: `AttachmentsSection.tsx` client (entre Card metadata y grid content/preview en `EditorView.tsx`) con grid 2-3 cols responsive de thumbnails (Next `<Image unoptimized>` con signed URLs pre-generadas server-side TTL 1h vía `createSignedAttachmentUrls`), caption inline editable con `onBlur` → action, reorder con flechas ↑↓ (NO dnd-kit en v1 — 17 KB + a11y complexity por 4-8 imágenes promedio, FU2 si el feedback lo demanda), delete con `AlertDialog` confirmation, lista vertical de archivos con botón "Descargar" via signed URL. `ConsultoraSettingsView.tsx` client en `/settings/consultora` con preview del logo + botones "Reemplazar logo" / "Eliminar logo" (gate owner-only con Alert "Solo el owner puede editar..." para members + botones disabled). Layout nuevo `src/app/(app)/settings/layout.tsx` placeholder para futuras tabs (perfil, billing). Sidebar nav suma item "Configuración" con icon `Settings` lucide (8 items totales: 2 live informe + 5 soon + 1 settings). `getCurrentConsultora` extendido con `logoStoragePath: string | null` propagado a layout `(app)` + Settings + PrintTemplate. **PDF integration** (PrintTemplate.tsx): header condicional con `<img class="pdf-brand-logo" max-height:48pt max-width:200pt object-fit:contain>` si hay `branding.logoSignedUrl`, fallback al wordmark con `consultora.name` (NO hardcoded "ConsultoraDemo"); sección "Anexos visuales" después del body markdown con `page-break-before: always` y por figura `page-break-inside: avoid` + `<img max-width:100% max-height:180mm object-fit:contain>` + figcaption "Imagen N — caption opcional"; sección "Anexos descargables" con tabla simple. Print page server fetcha attachments + genera signed URLs (TTL 5 min, holgura para Puppeteer setContent+pdf+buffer) + logo URL con mismo TTL — Puppeteer `waitUntil: 'networkidle0'` (ya seteado T-023) espera los fetches antes de page.pdf(). **Hallazgo crítico durante smoke local PDF** (sin esto el feature NO funciona en producción): el CSP del `(print)/layout.tsx` heredado de T-023 era `img-src 'self' data:`, que bloquea silenciosamente las signed URLs cross-origin al host `*.supabase.co` (Puppeteer respeta el CSP del response HTML). Síntoma: PDF salía con alt text + icono "broken image" donde debería estar el logo/foto. Fix: derivar el origin desde `NEXT_PUBLIC_SUPABASE_URL` y agregarlo dinámicamente al `img-src` server-side, con fallback `https:` si el env está malformado (más restrictivo que `*`). Bloque de comentario explícito en `(print)/layout.tsx:49-78` explicando el PORQUÉ. **T-024-FU2 entró al PR** ([docs/technical/06-deployment.md](docs/technical/06-deployment.md) sección "Chromium para PDF render"): documenta que dev local Windows/macOS necesita `CHROMIUM_PATH` apuntando a Chrome instalado (`puppeteer-core` no descarga binario propio); el Dockerfile alpine ya lo tiene seteado en `/usr/bin/chromium-browser` para prod. Tests E2E con PDF requieren el override también. **Tests**: 3 unit `attachment-validators` (38 tests: MIME whitelist por kind, magic bytes 7 formatos, size caps, sanitizeFilename con NFKD + path traversal + truncate preservando extension corta) + `storage-paths` (15 tests: extForMime, buildAttachmentPath/buildLogoPath con UUID asserts, parse round-trip) + `human-format` (10 tests: humanBytes 0→GB + negative/NaN, humanMime mapping); 3 integration `informe-attachments-rls` (13 tests: SELECT/INSERT/UPDATE/DELETE cross-tenant denied + happy paths + uploaded_by=auth.uid() spoof denied + cascade DELETE informe → 0 rows attachments + audit_log shape INSERT/UPDATE diff/UPDATE no-op) + `informe-attachments-actions` (17 tests: UUID malformado/sin sesión/sin consultora/cross-tenant 404/member non-editor 403/sin file 400/MIME unsupported 415/size > 10MB 413/magic bytes mismatch 415/happy path image con sharp real + audit log + storage object + happy path file PDF + position auto-incrementa + updateCaption happy + caption en file rechazado + reorder happy + delete con cleanup storage + delete member-non-creator FORBIDDEN) + `consultora-logo-actions` (9 tests: sin sesión + member non-owner FORBIDDEN + MIME PDF rechazado + size > 2MB + happy path owner + replace borra path previo con poll retry vía `list()` no `download()` por cache layer + remove action + idempotent sin logo + remove member FORBIDDEN); 3 E2E nuevos: `informes-attachments.spec.ts` (1 test: upload PNG 700x500 sharp inline + upload PDF + edit caption + upload 2da PNG + reorder con flecha up swap → segunda imagen pasa a posición 0 verificado via DB + delete del PDF con AlertDialog confirm + descargar PDF del informe con magic bytes %PDF- + size > 10KB), `informes-attachments-permissions.spec.ts` (1 test: member non-creator non-owner → `/editar` redirige a detail page + sin acceso a edición), `consultora-logo.spec.ts` (2 tests: owner upload logo + preview visible + DB column `logo_storage_path` populated + PDF post-logo descargable con magic bytes; member non-owner ve Alert "Solo el owner" + botones disabled). **Ajuste a test pre-existente** (NO eliminación): `src/tests/unit/AppSidebarNav.test.tsx:48` cambia `expect(items.length).toBe(7)` → `toBe(8)` por sumar item "Configuración" al sidebar (cero refactor destructivo, ningún test removido). **Hallazgo lateral durante sanity check 2** (no bloqueante): correr `pnpm test:integration` en bulk paralelo contra remote Supabase tiene flakiness pre-existente — algunos tests `auth-hook` + `informes-metadata-rls` fallan con `permission denied for function is_member_of_consultora` cuando concurren, pasan verdes en aislamiento + con `--no-file-parallelism` mejora pero no resuelve. Issue [#56](https://github.com/LautiRoveda/consultora-demo/issues/56) abierto cubriendo los 3 síntomas (signin + auth-hook + informes-metadata-rls). NO afecta CI (workers=1 ya seteado en `isCI`). Mismo patrón en bulk run E2E secuencial — los tests T-024 pasan aislados pero a veces uno en bulk. **Edge cases**: storage quota exceeded (server cuenta antes de INSERT, retorna 409 QUOTA_EXCEEDED con cap 20/informe), orphan files si DB insert falla post-upload (rollback storage delete inline + cron FU1 para cleanup tardío), imagen EXIF-rotada (sharp `.rotate()` la endereza), delete informe → cascade DB pero NO storage objects (cron FU1), logo eliminado sin replace (fallback wordmark `consultora.name`), cross-tenant access intent (RLS doble: tabla + storage.objects policies), race condition reorder (bulk UPDATE iterativo con verificación in_orderedIds + audit diff guard registra el delta), Supabase Storage tiene cache layer en `download()` (los tests usan `list()` para verificar delete inmediato sin flakiness). **Smoke productivo manual** post-merge: responsabilidad de Lautaro en `https://consultora-demo.test-ia.cloud` con los 7 puntos del plan section "Verificación end-to-end" (owner flow logo, member flow logo, upload imágenes con EXIF iPhone, upload archivos no-imagen, quota, cross-tenant defense, member non-editor flow). **Follow-ups esperados**: **T-024-FU1** `tech-debt` (cron admin de cleanup huérfanos Storage > 24h sin counterpart en DB — defensa contra rollback parcial), **T-024-FU2** ✅ (Chromium dev local doc — entró al PR via `06-deployment.md`), **T-024-FU3** `feature` (conversion HEIC/HEIF → JPG con libheif para fotos de iPhone sin convert manual), **T-024-FU4** `tech-debt` (quotas globales por tenancy cuando vengan planes pagos — hoy solo cap por-informe). **Sin nuevos shadcn primitives** (todos reusados de T-021/T-023: Card, Button, Input, Tooltip, AlertDialog, Collapsible, Separator, Alert). **Cifras suite**: **175 unit/component** (baseline 112 T-023-FU4 + 63 nuevos T-024) + **168 integration** (baseline 129 + 39 nuevos T-024) + **44 E2E** (baseline 40 + 4 nuevos T-024) + 1 skipped opt-in = **388 tests verdes** (+107 sobre baseline T-023-FU4).

- **Sprint 3 — Calendario + Notificaciones** 🚧 (en curso, 5 tickets completos)
  - **T-027** ✅ Migration base del módulo Calendario — schema-only ticket que sienta tablas + RLS + audit (server actions vienen en T-028, UI en T-029). Migration `supabase/migrations/20260514125515_calendar_events.sql`. **Tabla `public.calendar_events` (15 columnas)**: `id uuid pk default gen_random_uuid()` · `consultora_id uuid not null references consultoras(id) on delete cascade` · `tipo text not null check (tipo in (...))` con 7 valores `protocolo_anual|epp_entrega|capacitacion|calibracion|examen_medico|rgrl_anual|custom` · `titulo text not null check (length(trim(titulo)) between 3 and 200)` · `descripcion text check (descripcion is null or length(descripcion) <= 2000)` · `informe_id uuid references informes(id) on delete set null` · `fecha_vencimiento date not null` · `recurrence_months int check (recurrence_months is null or recurrence_months between 1 and 60)` · `status text not null default 'pending' check (status in ('pending','completed','cancelled'))` (sin `'snoozed'` por DA-04 — postergar es UPDATE de `fecha_vencimiento` real) · `completed_at timestamptz`+`completed_by uuid references auth.users(id) on delete set null` · `reminder_offsets_days int[] not null` (sin default SQL: T-028 server action inyecta el array correcto según tipo) · `metadata jsonb check (metadata is null or pg_column_size(metadata) <= 4096)` · `created_by uuid references auth.users(id) on delete set null` · `created_at`+`updated_at timestamptz not null default now()` con trigger `set_updated_at_calendar_events` que reusa `public.set_updated_at()` de T-011. **2 indexes**: `idx_calevents_consultora_fecha` partial `WHERE status='pending'` sobre `(consultora_id, fecha_vencimiento)` para query principal del calendario (mantiene el índice chico, completed/cancelled fuera del caso de uso); `idx_calevents_informe` partial `WHERE informe_id IS NOT NULL` para lookup T-036 modal post-firma. **Tabla `public.calendar_event_reminders` (8 columnas)**: `id uuid pk` · `event_id uuid not null references calendar_events(id) on delete cascade` · `consultora_id uuid not null references consultoras(id) on delete cascade` (denormalizado deliberadamente para RLS fast-path sin join al parent — mismo trade-off que T-024 `informe_attachments`) · `offset_days int not null check (offset_days >= 0 and offset_days <= 365)` · `scheduled_at timestamptz not null` · `status text not null default 'pending' check (status in ('pending','sent','skipped','failed'))` · `sent_at timestamptz` · `created_at` · `unique (event_id, offset_days)` (capa DB del idempotence stack del Discovery § 7.4 — previene dups si T-028 reintenta crear). **2 indexes**: `idx_reminders_due` partial `WHERE status='pending'` sobre `scheduled_at` (índice principal del cron T-031: "reminders due cuyo status sigue pending") + `idx_reminders_event` sobre `event_id` (lookup "reminders de este event" para UPDATE de fecha → recalc). **RLS calendar_events (3 policies)**: `calendar_events_select_own` USING `is_member_of_consultora(consultora_id)` (cualquier member de la consultora ve los eventos) · `calendar_events_insert_own` WITH CHECK `is_member_of_consultora(consultora_id) AND created_by = auth.uid()` (auto-atribuido, no usurpa identidad) · `calendar_events_update_own_or_owner` USING+WITH CHECK `is_member_of_consultora(consultora_id) AND (created_by = auth.uid() OR is_owner_of_consultora(consultora_id))` (creator OR owner). DELETE en events **default-deny para authenticated** (sin policy) — soft-delete UX = UPDATE status='cancelled'; hard-delete solo via service-role (cleanup admin / cascade desde consultoras). **RLS calendar_event_reminders (1 policy)**: `calendar_event_reminders_select_own` USING `is_member_of_consultora(consultora_id)` (transparencia para el user de "qué voy a recibir y cuándo"). INSERT/UPDATE/DELETE en reminders **SIN policy para authenticated** = service-role only (decisión deliberada: si abriera a authenticated, un user podría programar `scheduled_at` arbitrario o duplicar reminders bypassing la UNIQUE constraint). T-028 server actions crean reminders via `createServiceRoleClient()`; T-031 cron marcará `sent/skipped/failed` también via service-role. Helpers SQL T-015 (`is_member_of_consultora`, `is_owner_of_consultora`) reutilizados — **sin nuevas funciones SQL en T-027**. **Audit trigger `audit_calendar_events()`** `security definer set search_path = ''` (defensa contra search_path hijacking) + 3 triggers AFTER INSERT/UPDATE/DELETE escriben a `audit_log` con `actor_user_id = auth.uid()`. **Diff guard del UPDATE**: row de audit solo se escribe si `(titulo, tipo, status, fecha_vencimiento, recurrence_months, descripcion, completed_at) is distinct from (old.*)` — `metadata` y `reminder_offsets_days` deliberadamente excluidos del guard (cambios en ellos NO disparan audit row porque no son business-meaningful: metadata es payload para futuros campos custom + offsets son recomputables). `descripcion` está en el guard pero NO en el `jsonb_build_object` de `before_data`/`after_data` payload (puede pesar hasta 2 KB y satura el row de audit_log). Payload INSERT incluye `tipo,titulo,status,fecha_vencimiento,recurrence_months,informe_id`; UPDATE incluye `titulo,tipo,status,fecha_vencimiento,recurrence_months,completed_at`; DELETE incluye `titulo,tipo,status,fecha_vencimiento`. **NO hay audit trigger sobre `calendar_event_reminders`** (deliberado: operacional, no de negocio — el lifecycle sent/skipped/failed vive en su propia columna `status` + `sent_at`). Helpers T-015 reusados + sin nuevas funciones SQL en T-027. FKs a `clientes`/`empleados` omitidas porque esas tablas no existen aún (la migration que las cree agregará las columnas + FK + indexes). **Tests**: 19 integration en `src/tests/integration/calendar-events-rls.test.ts` cubriendo: 12 sobre events RLS (SELECT cross-tenant denied + SELECT member allows + INSERT member auto-attrib OK + INSERT cross-tenant blocked + INSERT con created_by spoof blocked + UPDATE creator OK + UPDATE owner non-creator OK + UPDATE member non-creator non-owner blocked (0 rows + título intacto) + UPDATE cross-tenant blocked + DELETE default-deny via authenticated + cascade consultoras→audit_log RESTRICT por FK + cascade event→reminders DELETE) + 3 sobre reminders RLS (SELECT member same-tenant OK + SELECT cross-tenant denied + INSERT authenticated blocked) + 4 sobre audit_log (INSERT escribe row con shape esperado + UPDATE diff-guard campo escribe row + UPDATE campo fuera del guard NO escribe row + DELETE escribe row con before_data). **Cifras CI** del merge SHA del run [25862705167](https://github.com/LautiRoveda/consultora-demo/actions/runs/25862705167): **175 unit/component + 44 E2E** verdes en CI (integration corre solo local: 168 baseline T-024 + 19 nuevos = 187). Suite total local: **175 + 187 + 44 = 406 tests verdes** (+19 sobre baseline T-024 — solo agregado, sin regresiones).
  - **T-028** ✅ Server actions CRUD eventos + reminders + tests integration — capa server-side primitiva del módulo Calendario. Co-localizado en `src/app/(app)/calendario/` siguiendo convención T-019/T-024 (NO `src/modules/calendario/` como decía el prompt original — esa carpeta no existe en este repo). **Archivos nuevos**: `actions.ts` (4 server actions con `'use server'`), `queries.ts` (5 queries de lectura), `schema.ts` (Zod input schemas SIN `'use server'` — se importa desde Client para zodResolver), `defaults.ts` (constantes: enums TS espejo de los CHECK SQL + `DEFAULT_REMINDER_OFFSETS_BY_TYPE` + bounds de validación + `SCHEDULED_AT_SEND_HOUR_LOCAL=9` + offset UTC-3 fijo). Plus helper compartido reusable por T-031 cron en `src/shared/calendar/scheduling.ts`. **4 server actions con discriminated union return** (patrón canónico T-019/T-024 — NUNCA throw, siempre return `{ok:true,...}|{ok:false,code,message,fieldErrors?}`): `createCalendarEventAction(input)` — Zod parse → getUser → getCurrentConsultora → resolve offsets (input o `DEFAULT_REMINDER_OFFSETS_BY_TYPE[tipo]`) → INSERT event con cliente authed (RLS WITH CHECK valida member + auto-attrib `created_by=auth.uid()`) → si Postgres devuelve code `42501` (RLS rejection cross-tenant o spoof) mapea a `FORBIDDEN` (alineado con T-024 attachment pattern) → bulk INSERT reminders via `createServiceRoleClient()` (RLS bloquea INSERT a authenticated en reminders, decidido T-027) → **rollback manual del event** si bulk INSERT falla post-event-INSERT (no hay transactions cross-client en Supabase, mismo patrón que T-024 storage) → `revalidatePath('/calendario')`+`/dashboard` → return `{ok:true, eventId, remindersCreated, remindersSkippedPast}`. `updateCalendarEventAction(eventId, patch)` — Zod (eventId UUID + patch refine "al menos un campo") → SELECT event vía cliente authed (RLS filtra cross-tenant → null=NOT_FOUND) → permission gate defensivo `isCreator || isOwner` (FORBIDDEN si false) → UPDATE event authed con `.select('id')` post-op para detectar race RLS WITH CHECK → **3 casos de recálculo reminders**: (A) patch trae `reminder_offsets_days` → DELETE reminders pending vía service-role + INSERT nuevos vía `computeReminderRows` (sent/failed previos se preservan); (B) patch trae solo `fecha_vencimiento` (sin offsets) → loop por reminders pending, recomputar `scheduled_at` con `computeScheduledAtUtc(newFecha, r.offset_days)`, UPDATE inline preservando `id+offset_days` (UNIQUE constraint intacta), si el nuevo `scheduled_at < now` → marcar `status='skipped'`; (C) patch no toca fecha ni offsets → no tocar reminders. Return `{ok:true, eventId, remindersRecomputed, remindersCreated, remindersSkippedPast}`. `completeCalendarEventAction(eventId)` — Zod UUID + auth + consultora + SELECT event → check `status === 'pending'` (sino `ALREADY_FINAL`) → permission gate creator OR owner → UPDATE `status='completed', completed_at=now(), completed_by=auth.uid()` → UPDATE pending reminders → `skipped` vía service-role (no spam post-complete) → **auto-recurrencia Opción A**: si `event.recurrence_months !== null` → INSERT next event con `addRecurrenceMonths(fecha, N)` + mismo `tipo,titulo,descripcion,recurrence_months,reminder_offsets_days,metadata` pero **`informe_id=null` en next** (decisión: el informe original es el doc del periodo actual, no el siguiente — DA-05 modal post-firma re-vincula con el informe N+1) + `created_by=auth.uid()` del completador (no preserva original) + nuevos reminders vía `computeReminderRows`. **Best-effort: si la auto-recurrencia falla NO revierte el complete** del original — el user vino a marcar "lo hice", no a "creá el siguiente"; errores van a Sentry vía `logger.error` con contexto suficiente (`originalEventId, fechaVencimiento, recurrence_months, err`) para recovery manual admin. Return `{ok:true, eventId, nextEventId, remindersSkipped, nextRemindersCreated, nextRemindersSkippedPast}`. `cancelCalendarEventAction(eventId, reason?)` — análogo a complete pero `status='cancelled'` + opcional reason en `metadata.cancel_reason` (merged con metadata previo) + UPDATE pending reminders → skipped + **NO crea next aunque haya `recurrence_months`** (cancelar != completar; anti-test cubierto). `cancel_reason` queda en metadata pero NO en audit_log (excluido del diff guard T-027), aceptable porque `status='cancelled'` ya queda en audit. **NO `deleteCalendarEventAction`** — RLS default-deny para authenticated por T-027 (soft-delete vía cancel). **5 queries de lectura** en `queries.ts` con `import 'server-only'` defensivo (sin `'use server'` para que sean importables desde Server Components + Server Actions): `getCalendarEventsForConsultora(supabase, options?)` paginado con filtros `status[]`+`tipo[]`+`fechaFrom`+`fechaTo`+`limit`+`offset`, ORDER BY fecha ASC + id; `getCalendarEventById(supabase, eventId)` devuelve `{event, reminders[]}` (2 queries separadas en lugar de embedded join para shape predecible + typed); `getUpcomingEvents(supabase, daysAhead=30)` para panel "próximos vencimientos" dashboard T-030 (BETWEEN today AND today+N); `getOverdueEvents(supabase)` status='pending' AND fecha < today; `getEventsByInformeId(supabase, informeId)` para T-036 modal post-firma (detectar si el informe ya tiene vencimiento auto-creado). **Todas reciben client authed** como primer arg → RLS automático filtra por consultora del JWT; NO toman `consultora_id` como param (fuente de verdad es el claim, no el caller). **Helper compartido `src/shared/calendar/scheduling.ts`** reusable por T-031 cron: `computeScheduledAtUtc(fechaIso, offsetDays)` construye `Date.UTC(year, month-1, day-offset, SCHEDULED_AT_SEND_HOUR_UTC=12, 0, 0)` evitando sorpresas del TZ del runtime (VPS Hostinger) — Argentina UTC-3 fijo sin DST desde 2009 → 09:00 ART = 12:00 UTC; setting per-consultora llega cuando entren tenants Chile/Uruguay Fase 5 → follow-up T-028-FU3. `computeReminderRows({eventId, consultoraId, fechaVencimientoIso, offsetDays[], now})` devuelve `{rows[], skippedPast: number}` — para cada offset construye row con `scheduled_at`, si `scheduledAt < now` skipea silencioso (NO persiste fila `status='skipped'` para mantener UNIQUE limpia para futuros UPDATE de fecha que reincorporen el offset; Discovery § 5.5). `addRecurrenceMonths(fechaIso, months)` wrapper de `date-fns/addMonths` (handle correcto fin-de-mes: Jan 31 + 1 = Feb 28/29 bisiesto, Mar 31 + 1 = Apr 30 — NO Mar 3 ni May 1 como Date nativo). **Defaults reminder offsets per tipo** (`DEFAULT_REMINDER_OFFSETS_BY_TYPE` en `defaults.ts`, alineado con Discovery § 5.2): `protocolo_anual [30,7,0]` (estándar industria), `rgrl_anual [60,30,7,0]` (crítico legal, 60d permite coordinar fecha con la ART), `epp_entrega [14,3,0]` (entrega es a 6 meses, 14d permite comprar stock + agendar visita), `capacitacion [30,7,0]`, **`calibracion [60,14,0]`** (laboratorio externo turnos largos — **fix sobre typo `[30,7,0]` del prompt original**, alineado con discovery), `examen_medico [30,7,0]`, `custom [7,0]` (vencimientos one-off del consultor). **Dep nueva**: `date-fns ^4.1.0` (~3KB tree-shake por `addMonths`; sin `date-fns-tz` — Argentina UTC-3 fijo se computa inline). **Tests**: 17 unit en `src/tests/unit/calendar-scheduling.test.ts` cubriendo `computeScheduledAtUtc` (UTC fijo, offset 0=mismo día a 12:00 UTC, cross-mes vencimiento día 5 con offset 30 cae mes anterior, cross-año vencimiento enero con offset 60 cae noviembre previo, offset grande 365=un año antes, hora UTC siempre 12:00 sin importar TZ del runtime), `computeReminderRows` (happy path todos futuros + skip silencioso offsets pasados + todos en pasado retorna 0 rows + propagación event_id+consultora_id + preserva orden del input sin reordenar), `addRecurrenceMonths` (12 meses mismo día año siguiente + Jan 31 + 1 = Feb 28 + Jan 31 + 1 = Feb 29 bisiesto 2028 + Mar 31 + 1 = Apr 30 + 6 meses cross-año + cap 60 meses = 5 años). 25 integration en `src/tests/integration/calendar-events-actions.test.ts` con fixtures real (2 consultoras + 4 users + cookieStore mockeado + sessionCache para signin reuso): `createCalendarEventAction` (3 happy paths defaults RGRL/EPP/calibracion → cifras exactas de reminders + override custom + skip pasado vencimiento +5d con default [30,7,0] → 1 reminder + 2 skipped con assertion del `logger.warn` capturado + member non-creator OK + INVALID_INPUT titulo de 2 chars + UNAUTHENTICATED + NO_CONSULTORA user huérfano), `updateCalendarEventAction` (update por creator + update por owner non-creator + update por member non-creator non-owner FORBIDDEN + update fecha → recompute scheduled_at por reminder pending verificado contra DB + update offsets → DELETE+INSERT con sent preservado + cross-tenant NOT_FOUND), `completeCalendarEventAction` (complete sin recurrence → status+completed_at+completed_by + reminders pending→skipped + nextEventId null; complete con recurrence_months=12 + informe vinculado fixture → next event creado con fecha+12 meses + tipo+titulo preserved + informe_id=null + created_by=completador + status=pending; complete ya completado → ALREADY_FINAL; audit_log row con `before.status='pending', after.status='completed'`), `cancelCalendarEventAction` (cancel happy path con reason → metadata.cancel_reason populado + reminders skipped; cancel sin reason → metadata sin cancel_reason key; cancel NO crea next event aunque haya recurrence_months — anti-test verifica count de events sin cambios), `queries` (getUpcomingEvents(7) filtra +1d/+5d sí y +60d no; getOverdueEvents devuelve solo pending vencidos NO completados-overdue; cross-tenant cB no ve eventos de cA via getCalendarEventById null + getUpcomingEvents excluye). **Cifras CI** del merge SHA del run [25893455643](https://github.com/LautiRoveda/consultora-demo/actions/runs/25893455643): **192 unit/component + 44 E2E** verdes en CI (integration corre solo local: 187 baseline T-027 + 25 nuevos = 212). Suite total local: **192 + 212 + 44 = 448 tests verdes** (+42 sobre baseline T-027). **Follow-ups abiertos**: T-028-FU1 tabla `consultora_calendar_defaults` per Discovery § 5.4 (override jerárquico tipo > consultora > evento), T-028-FU2 retry async de auto-recurrencia si falla en prod (hoy es síncrono best-effort + Sentry), T-028-FU3 hora envío preferida per-consultora (hoy hardcoded 09:00 ART), T-028-FU4 helper `executeWithRollback` si más de 2 actions futuras lo necesitan (hoy rollback inline es OK).
  - **T-029** ✅ UI Calendario mensual + form crear/editar — primer feature visual del módulo Calendario, vista mensual con drawer reusable para CRUD + filtros + navegación cross-mes preservando URL state. **Archivos nuevos (12 en `src/app/(app)/calendario/`)**: `page.tsx` (server component: parsea `searchParams`, valida sesión + consultora, ejecuta `getCalendarEventsForConsultora` con `monthBoundsIso` + filtros, redirige `no_consultora` → dashboard, pasa data + `currentUserId` + `currentUserRole` al cliente), `CalendarView.tsx` (client orquestador: state machine del drawer + URL state via `router.replace`, callbacks `openCreateOnDay`/`openViewEvent`/`openEditEvent`/`closeDrawer`/`changeMonth`/`applyFilters`/`handleMutated`, envuelve todo en `TooltipProvider` para que tooltips de Tabs+EventViewPanel funcionen), `CalendarMonthView.tsx` (grid 7-col custom L-D **NO shadcn calendar para la grid** — shadcn calendar es date-picker primitive, no calendario; padding del mes anterior/siguiente hasta completar semana, `+N más` via Popover cuando >3 eventos/día), `CalendarMonthCell.tsx` (cell individual: header día + lista bullet de eventos con `data-testid="event-<uuid>"`, badge variants según estado), `EventDrawer.tsx` (Sheet shadcn reusable, state machine 4 modos `closed | view(eventId) | create(fechaPrepop) | edit(eventId)`, look-up del event por id desde la lista pasada por prop — no fetch adicional; `overflow-y-auto` crítico mobile), `EventViewPanel.tsx` (read-only modo view, calcula `canEdit` + permission gate + tooltip "Solo el creador o un owner..." para members non-creator-non-owner), `EventForm.tsx` (RHF + zodResolver, defaults dinámicos según `mode` + warning offsets pasados + date picker shadcn Calendar dentro de Popover, `useWatch` en lugar de `form.watch` por React Compiler), `EventStatusActions.tsx` (botones Completar/Cancelar con AlertDialog confirm, toast con CTA "Ver siguiente vencimiento" post-recurrencia), `ReminderOffsetsInput.tsx` (chips editables `[60d antes] [7d antes] [El día]`, Enter agrega + click X remueve + cap 6, prepop reactivo al cambiar `tipo` SI `dirty=false`), `CalendarFilters.tsx` (multi-select tipo+status via Popover, batch commit al cerrar el popover — evita N `router.replace` por checkbox toggleado), `labels.ts` (`EVENT_TIPO_LABELS` es-AR + `EVENT_STATUS_LABELS` + helper `formatRecurrence(months)` "Cada año"/"Cada N meses"), `url-state.ts` (parser/builder puro testeable: `parseUrlState` con regex YYYY-MM + UUID gate + intersect con `EVENT_TIPO_VALUES` + dedup, `buildSearchParams` omite defaults para URL canónica, `addMonthsToYM`+`formatYM`+`monthBoundsIso` aritmética modular limpia). Plus `event-form-helpers.ts` (helpers puros: `dateToCivilIso`/`civilIsoToDate`/`findOffsetsInPast`). **URL state shareable** `?month=YYYY-MM&tipo=a,b&status=c,d&event=<uuid>` — solo `event` persiste en URL (intent compartible "viste este vencimiento"). Create/edit son intents efímeros del user que F5 descarta a propósito (similar a refresh perdiendo un input no-submiteado). **Drawer state derivado** (no doble fuente de verdad): `viewEventId` viene del `searchParams.get('event')` directamente; `intent` local solo para create/edit. Evita la trampa "setState dentro de useEffect" que ocurre al sincronizar dos fuentes — el lint rule `react-hooks/set-state-in-effect` lo flag (problema descubierto + fixed en pre-commit). **State machine transitions del drawer**: click "+ Nuevo" → `intent={mode:'create',fechaPrepop:null}` · click día vacío → `intent={mode:'create',fechaPrepop:cellIso}` · click evento → `pushSearchParams({event:id})` (sin intent) · click "Editar" en view → `intent={mode:'edit',eventId}` · click X/overlay/Esc → `setIntent(null)+pushSearchParams({event:null})` · cambio de mes → cierra drawer + push month · submit edit OK → `switchToView` (intent=null + push event=id) · complete con recurrencia → cierra drawer + toast con CTA → click CTA → push month=next + event=nextId (open view del next). **Render strategy del grid**: `eachDayOfInterval(startOfMonth, endOfMonth)` con offset lunes-inicio (convierte `getDay` ARG dom=0 → lunes=0 con módulo); cells del padding del mes anterior/siguiente pintan `bg-muted/30 text-muted-foreground` pero preservan badge color por estado; sort intra-día: vencidos primero (status='pending' AND fecha < today), después pending future por id ASC, después completed/cancelled al final. Click cell vacía dispara `onClickDay`; click evento dispara `onClickEvent` con `stopPropagation` (NO dispara `onClickDay`). Día con > 3 eventos muestra 3 visibles + chip `+N más` que abre Popover con la lista completa. **Badge variants** por estado en `CalendarMonthCell:badgeClassesFor()`: `completed` → emerald 500/15 con line-through · `cancelled` → muted con opacity-60 + line-through · `pending AND fecha < today` → destructive 500/15 (vencido) · `pending` future → primary 500/15. **Permission gate (creator OR owner)** matchea backend T-028: EventViewPanel calcula `canEdit = event.created_by === currentUserId || userRole === 'owner'`; si false → 3 botones disabled (Editar + Completar + Cancelar) + tooltip "Solo el creador o un owner pueden modificar este vencimiento". Test cubre los 3 casos (creator, owner non-creator, member non-creator non-owner). **Warning hora 09:00 ART (ajuste 3 del plan)**: helper puro `findOffsetsInPast(fechaVenc, offsets, now)` reusa `computeScheduledAtUtc` de T-028 (12:00 UTC = 09:00 ART) y devuelve subset de offsets cuyo `scheduled_at < now`; EventForm muestra `<Alert>` cuando array no vacío recomputado en `useMemo([watchedFecha, watchedOffsets])`. Previene UX leak "reminder skipped silencioso" — útil hoy aunque el cron T-031 todavía no envíe; el backend ya skipea los reminders pasados (Discovery § 5.5), la UI ahora le avisa al user antes del submit. **TZ bug fix date picker (ajuste 5)**: helper `dateToCivilIso(date)` con `format(date, 'yyyy-MM-dd')` de date-fns en lugar de `date.toISOString().slice(0,10)`. `toISOString()` convierte a UTC primero — si browser TZ ≠ UTC, el user clickea 15-jun pero el ISO sale con día -1 (UTC+12 NZ) o día +1 (UTC-3 ART en horario lejano del día). `format()` lee local TZ del Date, matchea siempre el día que el user clickeó. +5 unit tests con Date construido en TZ local incluyendo bisiesto + último día año + edge cases. **shadcn components nuevos** instalados con `pnpm dlx shadcn@latest add calendar popover badge tabs`. Los 4 ya respetan el patrón `radix-ui` unificado del repo (sin `@radix-ui/react-*` separados, mismo patrón que `sheet.tsx` desde T-017). Única dep peer agregada: `react-day-picker ^10.0.0`. Convención documentada inline en [`popover.tsx:5-15`](src/shared/ui/popover.tsx#L5-L15) explicando qué hacer si shadcn cambia el patrón en el futuro (regrep `@radix-ui/react-` post-add + reescribir a `radix-ui` unificado + NO sumar paquete separado a `package.json`). Fix puntual en `calendar.tsx`: removida línea `table: "..."` del slot ClassNames (rdp@10 eliminó ese slot, shadcn quedó out of sync con la nueva versión del peer). **Bug productivo encontrado y fixed pre-PR durante E2E del test 6 (recurrencia)**: sonner toast con CTA "Ver siguiente vencimiento" (que aparece tras complete de evento recurrente) era interceptado por Sheet overlay → CTA inaccesible. Síntoma: `locator.click()` con timeout 30s + Playwright reporta "data-slot=sheet-content intercepts pointer events". Fix UX correcto (no técnico-workaround): cerrar drawer ANTES de mostrar el toast porque la sesión del user sobre el evento original termina al marcarlo completado, mantenerlo abierto confunde + `duration: Infinity` en toast con CTA (sin esto sonner auto-cierra ~4s y el CTA se pierde antes que el user pueda decidir si quiere navegar al next event). Cambio aplicado en `EventStatusActions.tsx:onComplete` con comentario inline explícito explicando ambos motivos (UX + técnico). **`Tooltip` necesita `TooltipProvider`** — descubierto durante el primer E2E (TODOS los tests fallaron con "Tooltip must be used within TooltipProvider"). El AppShell ya wrappea `<AppSidebarNav>` con uno pero el contenido del shell NO heredaba. Fix: agregado al root del `CalendarView` envolviendo `EventDrawer`+`EventViewPanel`+`Tabs`. **Refactor pre-commit**: `form.watch()` de RHF dispara warning React Compiler "Use of incompatible library" (memoización inestable) que el pre-commit hook (`--max-warnings=0`) bloquea. Migrado a `useWatch({ control, name })` de RHF (4 calls afectados en EventForm: `tipo`, `fecha_vencimiento`, `reminder_offsets_days`, `recurrenceEnabled`). API estable + sin warning. **Sidebar nav**: item `/calendario` pasó `status: 'soon'` → `status: 'live'`. Sacó `ticket: 'T-023'` (drift legacy — el roadmap viejo del repo tenía T-023 = calendario, pero el real es T-029). Otros items soon con tickets viejos (T-020..T-024 labels en clientes/empleados/EPP/notificaciones) NO se tocaron — fuera de scope T-029. Test `AppSidebarNav.test.tsx` actualizado: lista `expectedSoonLabels` ahora tiene 4 elementos (sacó Calendario, sumó assertion explícita "Calendario es link live"), total sigue 8 items. **TODO inline** en [`EventViewPanel.tsx:44-47`](src/app/(app)/calendario/EventViewPanel.tsx#L44) apunta a **T-036**: copy "auto-creado por recurrencia" cuando el next event fue generado por `completeCalendarEventAction` requiere columna `parent_event_id uuid references calendar_events(id)` o `created_via_recurrence boolean` en `calendar_events`. La heurística sin schema (`informe_id IS NULL AND fecha_vencimiento > created_at + N días`) da falsos positivos comunes en eventos custom a futuro lejano. Diferido al ticket que cubre integración Informes ↔ Calendario (T-036 ya planeado con modal post-firma DA-05). **Decisiones que NO requirieron ADR**: URL state shareable (convención UI), drawer reusable modes (patrón de Sheet shadcn aplicado), react-day-picker v10 peer dep (consecuencia de shadcn calendar 2026), radix-ui unificado (ya documentado inline desde T-008). **Tests**: 73 unit/component nuevos desglosados: `CalendarMonthView.test.tsx` (9 tests: grid 7-col + headers L-D + agrupamiento por día + click handlers con stopPropagation + completed line-through + pending vencido destructive variant + `+N más` chip + padding cells del mes anterior + Junio 2026 lunes 1 cells correctos), `EventDrawer.test.tsx` (8 tests: 4 modos render + permission gate creator allows + owner non-creator allows + member non-creator non-owner blocks los 3 botones; mocks de server actions + ResizeObserver/PointerCapture stubs para jsdom + sonner stub), `ReminderOffsetsInput.test.tsx` (9 tests: chips iniciales + remove + Enter agrega + duplicado warning toast + valor fuera de rango error toast + cap 6 disable input + prepop reactivo dirty=false reemplaza chips + dirty=true preserva chips manuales + lista vacía mensaje "Sin recordatorios"), `calendar-url-state.test.ts` (26 puros: parser defaults + month YYYY-MM válido + month invalid fallback + month=YYYY-13 fallback + tipo intersect+dedup + tipo con valor inválido lo filtra + status default + status vacío → default + event UUID válido vs inválido + URLSearchParams real + Next 16 string[] shape + buildSearchParams omite defaults + tipo+status+event emitidos + addMonthsToYM cross-año adelante/atrás + delta grande + formatYM padding + monthBoundsIso febrero bisiesto vs no-bisiesto + diciembre 31 días), `calendar-labels.test.ts` (7 puros: exhaustividad EVENT_TIPO_LABELS cubre los 7 + EVENT_STATUS_LABELS cubre los 3 + formatRecurrence null/1/12/multiplo-de-12/no-multiplo), `event-form-helpers.test.ts` (13 puros: dateToCivilIso con bisiesto + último día año + hora alta del día + 00:01 no retrocede + roundtrip civilIsoToDate → dateToCivilIso preserva ISO + findOffsetsInPast todos futuros vacío + offset 30 cae pasado now < send hour + offsets pasado now > 12 UTC + todos pasado + orden preservado + input vacío). Plus +1 nuevo en `AppSidebarNav.test.tsx` (Calendario es link live). **6 E2E nuevos en `calendario-mensual.spec.ts`** con `workers=1` config CI: (1) happy path crear via drawer click día → tipo Select combobox → titulo + checkbox Recurrente → submit → toast OK + evento visible en cell + persistencia DB; (2) filtro por tipo aplicado oculta otros del cell; (3) navegar a mes siguiente preserva filtros en URL (`?month=YYYY-MM&tipo=...`); (4) complete simple → status='completed' + completed_at + completed_by; (5) cancel con motivo → status='cancelled' + metadata.cancel_reason; (6) **complete con recurrencia → toast CTA → next event en mes siguiente** (lookup admin del nextEvent post-test para track cleanup + verifica `recurrence_months=12` preservado). Test 6 corrió 3x con `--repeat-each=3` localmente sin flake (3/3 verde) + suite completa con `workers=1` (config CI) 6/6 verde. La flakiness pre-existente en bulk paralelo local (issue [#56](https://github.com/LautiRoveda/consultora-demo/issues/56)) NO afecta CI. **Cifras CI** del merge SHA `3425384` (run [25896503999](https://github.com/LautiRoveda/consultora-demo/actions/runs/25896503999)): **265 unit/component + 50 E2E = 315 tests verdes en CI** (integration corre solo local: +212 → suite full local **527 tests**). Sin migrations nuevas (todo el schema viene de T-027). Follow-ups: **T-036** (auto-crear vencimiento al firmar informe + columna `parent_event_id`/`created_via_recurrence` + copy recurrencia en EventViewPanel + modal post-firma DA-05).
  - **T-030** ✅ UI agenda lista + panel dashboard "Próximos vencimientos" — cierra capa UI del módulo Calendario, completa los puntos de entrada complementarios al calendario mensual T-029. PR `#69` merge SHA `6f06b73`. **Archivos nuevos en `src/app/(app)/calendario/`**: `layout.tsx` (server layout compartido con header h1 + descripción + child client `CalendarTabsNav`), `CalendarTabsNav.tsx` (client: tabs `[Mensual] [Agenda]` como `<Link>` plano + clases manuales — NO shadcn Tabs, derivar `value` de pathname implica wrapper innecesario; `usePathname()` pinta tab activo via `aria-selected` + `aria-current`), `agenda-buckets.ts` (helper puro reusable: `groupEventsByBucket(events, now)` clasifica eventos pending en 4 buckets — `hoy` fecha<=today / `siete` today<fecha<=today+7 / `treinta` today+7<fecha<=today+30 / `masAdelante` fecha>today+30; cada día cae en exactamente un bucket por bordes inclusive/exclusive; sort intra-bucket fecha ASC + id ASC; eventos status≠pending silent drop), `EventAgendaCard.tsx` (client: card individual con body clickable → onClickBody + footer 2 botones inline Completar [AlertDialog confirm] + Editar; permission gate creator OR owner matchea backend T-028 — member non-creator non-owner ve botones disabled + tooltip "Solo el creador o un owner..." replicando patrón EventViewPanel T-029; NO reuso EventStatusActions porque su API supone drawer + agrega Cancel inline indeseado), `agenda/page.tsx` (server: parsea searchParams, decide modo dual `buckets | flat`, fetch + delegate a AgendaView), `agenda/AgendaView.tsx` (client orquestador: state machine drawer + URL state + buckets render con `BucketSection` co-localizado + collapsible "Más adelante" default closed + `EmptyState` cuando todos vacíos + `FlatList` para modo flat). **Archivos nuevos en `src/app/(app)/dashboard/`**: `ProximosVencimientosPanel.tsx` (server async component embebido en `DashboardView.tsx` arriba del header: 2 queries paralelas — `getUpcomingEvents(30)` + `getOverdueEvents` — pasadas a `groupEventsByBucket` reusado del helper de agenda; 3 CountRow Hoy [destructive] / 7d [primary] / 30d [muted] + "Más urgente" deriva del primer evento del primer bucket no-vacío → link `/calendario/agenda?event=<uuid>`; "Ver todos →" link a `/calendario/agenda`; empty state fallback con CTA "Crear vencimiento" → `/calendario`). **Modificaciones**: `CalendarView.tsx` (quita header h1 + descripción + Tabs internos — van al layout; el CTA "Nuevo vencimiento" se queda inline al lado del nav de mes + filtros porque dispara state interno client del drawer y el layout server no puede comunicar con él), `EventDrawer.tsx` (suma `export type DrawerState` que antes vivía en `CalendarView` — DrawerState es propiedad natural de EventDrawer, limpia dependencia circular sibling-vs-sibling cuando AgendaView lo importa), `queries.ts` (suma `getEventsBeyondDays(supabase, daysFrom=30, limit=200)` para bucket "Más adelante" — partial filter `status='pending' AND fecha > today+N`, sort ASC, limit 200 para cap saturación), `DashboardView.tsx` (suma `<ProximosVencimientosPanel />` arriba del `<header>` "Bienvenido a ConsultoraDemo"; saca card "Calendario" del bloque "Próximamente" — drift legacy `ticket: 'T-023'` ya está live; grid `lg:grid-cols-4` → `lg:grid-cols-3`; cards Informes/Clientes/EPP **NO se tocan** — drift legacy fuera de scope). **Decisión 10 (refactor `DrawerState`)**: el tipo migra de `CalendarView.tsx` a `EventDrawer.tsx` (su dueño natural). 3 archivos modificados — EventDrawer define+exporta, CalendarView importa, `EventDrawer.test.tsx` import path updated. Sin impacto de runtime, ningún test removido. **Decisión 11 (`currentMonth` en agenda)**: `EventDrawer/EventForm/EventStatusActions` requieren `currentMonth` para decidir si navegar cross-month post-submit. En agenda no hay "mes mostrado" — `AgendaView` deriva `currentMonth` del YM del evento abierto (o today si no hay evento) y **ignora `gotoMonth` callback** en `handleMutated` (la agenda no navega por mes; respetamos solo `closeDrawer`/`gotoEventId`/`switchToView`). API de EventForm/EventStatusActions **intacta** — sin breaking changes. **Decisión 12 (modo dual buckets vs flat)**: `agenda/page.tsx` deriva server-side `mode: 'buckets' | 'flat'` desde los filtros activos: (A) status incluye `pending` (solo o mixed) → buckets normal, eventos completed/cancelled del filtro mixed se DROPEAN silenciosamente del render por `groupEventsByBucket` (status≠pending continue); (B) status NO incluye `pending` (solo completed, solo cancelled, o ambos) → modo flat con `getCalendarEventsForConsultora` + `[...].reverse()` en memoria (DESC para historial reverso, mas recientes arriba — cap 200, costo despreciable; NO se extendió el helper con `order` param). Default `status=['pending']` → siempre arranca buckets. **Decisión 13 (empty state panel CTA)**: link a `/calendario` (vista mensual canónica del módulo, click día vacío → crear es el flujo principal del wireframe Discovery § 8.1), NO `/calendario/agenda?create=1` con drawer pre-abierto (fricción técnica sin valor incremental). **`groupEventsByBucket` single source of truth**: reusado por AgendaView (renderiza 4 secciones) Y por ProximosVencimientosPanel (counts + "más urgente"). El panel pasa `[...overdue, ...upcoming]` agregado; bucket `masAdelante` queda siempre vacío en el panel porque `getUpcomingEvents(30)` cubre solo +30d — `masAdelante` solo aparece poblado en la agenda donde se llama `getEventsBeyondDays(30)`. **`addDaysIso(iso, days)`** exportado del módulo para tests; suma días positivos/negativos respetando rollover mes/año/bisiesto. **Habilitó tab `[Agenda]` en `CalendarView`**: pasó de disabled + tooltip "Próximamente (T-030)" → `<Link>` real navegable a `/calendario/agenda`. Tabs gestionados desde el layout — ambos vistas (mensual/agenda) heredan el chrome compartido. **Permission gate inline `EventAgendaCard`**: replica patrón `EventViewPanel` T-029 con tooltip envuelto en `<span>` para que Radix lo renderee sobre `<Button disabled>` (los nativos disabled no disparan eventos pointer). **Render strategy modo buckets**: cada `<BucketSection>` con título + count Badge + variant color matcheado (destructive/primary/muted); "Más adelante" con `collapsible: true, defaultOpen: false` para no abrumar visualmente con cap 200 eventos a futuro lejano. Empty state se renderiza SOLO si `totalBucketsCount === 0`. **Bug productivo encontrado y fixed pre-PR durante E2E del panel dashboard**: fixture del test `dashboard-vencimientos-panel.spec.ts #1` violaba CHECK constraint `calendar_events_titulo_check` (`length(trim(titulo)) between 3 and 200`) con títulos `"5d"` (2 chars) y `"20d"` (3 chars OK — sólo `"5d"` falló). Síntoma observado: `count-siete = 0` cuando esperaba `1` — el insert silent-failed con Postgres error code `23514`. Diagnóstico vía script de debug temporal (`scripts/dev-debug-panel.ts`) que ejecutó el insert con admin client + capturó el error completo. Fix de 1 línea en el fixture (`"5d"` → `"En 5d"`, `"20d"` → `"En 20d"`). Bug del test, NO del código. Script de debug temporal + log temporal en el panel eliminados antes del commit. **Riesgo TZ documentado inline en `agenda-buckets.ts`**: off-by-one posible en eventos con `fecha=today` durante horas 21:00-23:59 ART (cuando UTC ya es day+1). Aceptable MVP — `fecha_vencimiento` es civil date sin hora y el cron T-031 enviará reminders a 09:00 ART (12:00 UTC), bien fuera de ese hot window. Mismo issue diferido a T-028-FU3 (TZ per-consultora). **URL state agenda**: reuso de `parseUrlState`/`buildSearchParams` de T-029 — campos `year`/`month` ignorados en este contexto, `buildSearchParams` ya tiene "omit default" logic así que no contaminan la URL canónica de agenda. URL canónica `/calendario/agenda` o `/calendario/agenda?event=<uuid>` o `/calendario/agenda?tipo=protocolo_anual,rgrl_anual&status=pending,completed`. **Decisiones que NO requirieron ADR**: layout compartido + tabs como Link plano (convención UI), modo dual buckets/flat (consecuencia del filtro status), panel server async embebido (RSC nesting estándar de Next 16). **Tests**: 35 unit/component nuevos desglosados: `agenda-buckets.test.ts` (18 puros: addDaysIso cross-mes/año/bisiesto + groupEventsByBucket cada borde inclusive/exclusive con `fixedNow` deterministico + sort intra-bucket + silent drop completed/cancelled + cross-year + bisiesto 2028 + arreglo vacío + mix completo 1 por bucket), `EventAgendaCard.test.tsx` (6 component: render badge tipo+título+fecha+sin badges si futuro + "Vencido" overdue + "Hoy" today + click body dispara onClickBody sin disparar footer onClickEdit + permission gate member non-creator non-owner disabled + permission gate owner non-creator habilita), `AgendaView.test.tsx` (5 component: 4 buckets render + bucket vacío NO se renderiza + empty state si todos vacíos + mode='flat' renderiza lista plana NO buckets + CTA "Nuevo vencimiento" abre drawer mode=create), `ProximosVencimientosPanel.test.tsx` (6 component server async via mock `createClient` + `getUpcomingEvents` + `getOverdueEvents` → invocan async + render: counts correctos con eventos mixed 1+1+2+3 + "más urgente" overdue prioriza más viejo + "más urgente" sin overdue es el más próximo + empty state CTA → /calendario + link "Ver todos" → /calendario/agenda + sin user devuelve null). **3 integration nuevos** en `calendar-events-actions.test.ts` describe `queries`: `26. getEventsBeyondDays(30)` rango correcto (today+15/+30/+31/+90 → solo +31 y +90 aparecen, strict gt excluye +30), `27. excluye completed/cancelled` (anti-test), `28. respeta RLS cross-tenant`. **6 E2E nuevos**: `calendario-agenda.spec.ts` (4: navegación tab Mensual→Agenda + crear evento aparece en bucket correcto + filtro tipo aplicado + URL preserva tras refresh; completar inline desde card → card desaparece + status='completed' en DB; modo flat status=completed sin pending → lista plana NO buckets visible; permission gate member non-creator non-owner ve botones disabled), `dashboard-vencimientos-panel.spec.ts` (2: counts > 0 + más urgente=overdue más viejo + "Ver todos" → /calendario/agenda; empty state → CTA → /calendario). E2E completo del módulo Calendario verde con `workers=1` (config CI): **12/12 verdes** (6 mensual T-029 sin regresión + 4 agenda + 2 panel). Flakiness pre-existente en paralelo local issue [#56](https://github.com/LautiRoveda/consultora-demo/issues/56) NO afecta CI. **Cifras CI** del merge SHA `6f06b73` (run [25915944939](https://github.com/LautiRoveda/consultora-demo/actions/runs/25915944939)): **300 unit/component + 56 E2E = 356 tests verdes en CI** (integration corre solo local: +3 nuevos → suite full local +44). Sin migrations nuevas (todo el schema viene de T-027). **Smoke productivo manual** post-merge OK en `https://consultora-demo.test-ia.cloud` (verificado por Lautaro). **Follow-ups**: ninguno nuevo de T-030. T-028-FU3 (TZ per-consultora) ya estaba abierto pre-T-030.
  - **T-031** ✅ Cron pg_net + dispatcher reminders + canal Email (Resend) — primera notificación productiva del módulo Calendario, cierra capa server-side completa end-to-end. **T-031 absorbió scope T-032** (canal Email/Resend) decidido por el orquestador para entregar feature end-to-end funcional en lugar de stub: cuando un `scheduled_at` cae, el cron pg_cron lo claim-ea via SQL function, dispara POST async via pg_net a un endpoint Next.js, y el endpoint orquesta envío a los canales habilitados del user (email real via Resend; Telegram/Push siguen como stubs hasta T-033/T-034). PR `#71` merge SHA `e05c537`. **Arquitectura**: migration `20260515095701_notifications_infrastructure.sql` con `pg_net` extension + 2 tablas nuevas (`notification_channel_prefs` per-user + `notification_log` immutable bitácora) + RLS default-deny + audit_log immutability pattern de T-011 replicado para notification_log + trigger `AFTER INSERT en consultora_members` que crea row default `email enabled=true` + backfill retroactivo para members pre-T-031 + 2 secrets de Vault (`cron_dispatch_secret` + `cron_dispatch_base_url`) con NAME estable + función SQL `process_pending_reminders()` `security definer set search_path=''` con `SELECT FOR UPDATE SKIP LOCKED` + `cron.schedule('process-pending-reminders', '*/5 * * * *', ...)` con limit 100/tick (28k/día margen). Migration helper `20260515100457_set_cron_vault_secret_helper.sql` con `public.set_cron_vault_secret(name, value)` allowlisted a 2 nombres + `grant execute to service_role` only — útil para tests integration (que necesitan reescribir Vault sin Studio UI) + futura rotación programática. **Módulo nuevo `src/shared/notifications/`** (8 archivos): `resend.ts` singleton lazy patrón T-020 anthropic, `types.ts` con `DispatchResult` discriminated union + `ChannelOutcome` + `ReminderWithEvent` shape, `email-templates/reminder-vencimiento.ts` con HTML inline patrón T-079 (container 600px, paleta indigo `#4f46e5`, system stack, preheader `display:none mso-hide:all`, `<meta name="color-scheme" content="light">`, CTA button + fallback URL + highlight box + footer unsubscribe) + helpers `escapeHtml` (XSS defense para titulo/descripcion/recipientName) + `formatFechaEs` (YYYY-MM-DD → "DD de Mes de YYYY") + `buildSubject` (offset=0 → "HOY vence" / offset>0 → "Vence en N días") + text fallback plain readable, `senders/email.ts` con `sendEmailReminder` real via Resend SDK + `idempotencyKey: reminder.id` (idempotency capa 4) + mapping de error name → `RESEND_<NAME>` code, `senders/telegram.ts` + `senders/push.ts` stubs que retornan `{ok:false, errorCode:'NO_CHANNEL_IMPL_T033'}` / `T034` (mapped a `status='skipped'` no `'failed'` en el dispatcher), `dispatch.ts` orquestador puro `dispatchReminderToChannels` que itera los 3 canales en serie + lee `notification_channel_prefs` del recipient + skip enabled=false (DISABLED, NO log row — ruido) / muted_until>now (MUTED, NO log row) / `notification_log` lookup ALREADY_SENT (idempotency capa 3) / NO_RECIPIENT_EMAIL si email null + persist log row por canal con outcome. **Endpoint `POST /api/calendar/dispatch-reminder/route.ts`**: auth via header `X-Internal-Cron-Secret` matching `env.INTERNAL_CRON_SECRET` → Zod parse body `{reminder_id: UUID}` → service-role client (creado nuevo per request, NO el singleton client-scoped) lee reminder + event embebido + auth.users del recipient + channel_prefs → defensa `event.status !== 'pending'` (skipped EVENT_NOT_PENDING + log + 200, no error: el UPDATE 'sent' del lado cron ya pasó, at-most-once aceptado) → defensa `event.created_by IS NULL` (user deleted con FK `ON DELETE SET NULL` → skipped NO_RECIPIENT + log + 200, ajuste 3 del plan) → delegate al dispatcher puro → return JSON con `channels: ChannelOutcome[]` para debug visibility. **4 env vars nuevas en `src/env.ts`** (server-only): `RESEND_API_KEY: z.string().min(1)`, `RESEND_FROM_ADDRESS: z.string().email()`, `RESEND_REPLY_TO_ADDRESS: z.string().email().default('noreply@mail.consultora-demo.test-ia.cloud')` (ajuste 1 del plan: env var con default sensato evita hardcode de email personal en el sender), `INTERNAL_CRON_SECRET: z.string().min(32)`. Dep nueva: `resend ^6.12.3` (server-only). **Idempotency cascade 4 capas** (discovery § 7.4) implementada: (1) UNIQUE constraint `(event_id, offset_days)` ya en T-027 DB layer; (2) `UPDATE status='sent'` en la **misma transacción** que el SELECT FOR UPDATE SKIP LOCKED, antes del `net.http_post` — at-most-once delivery, claim layer cron SQL; (3) dispatcher chequea `notification_log` por `(reminder_id, channel, status='sent')` antes de emitir — sender layer; (4) Resend SDK recibe `idempotencyKey: reminder.id` por request — provider layer dedupea ventana 24h. **Decisiones implementadas**: Vault con NAME estable (`cron_dispatch_secret` / `cron_dispatch_base_url`) leído con `select decrypted_secret from vault.decrypted_secrets where name = '…'` — NO por UUID auto-generado por `vault.create_secret`, identificador robusto cross-environment + placeholder sentinela `'REPLACE_ME_POST_DEPLOY'` que la función SQL detecta y returnea early con `raise notice` (no `raise exception` — el cron seguiría intentando cada 5 min; con notice queda en logs Postgres sin alertar Sentry hasta que Lautaro configure los secrets reales) + `cron_dispatch_base_url` default `https://consultora-demo.test-ia.cloud` editable para staging/preview + at-most-once delivery (UPDATE status='sent' antes del HTTP; si HTTP falla, no reintenta — log a notification_log con `failed` + Sentry capture, notification no es critical path) + cron frequency `*/5 * * * *` cerrada en discovery § 7.2 (sweet spot carga vs jitter UX) + grants explícitos `usage on schema vault to postgres` + `select on vault.decrypted_secrets to postgres` + `usage on schema net to postgres` + `execute on all functions in schema net to postgres` (defensa contra missing permissions en proyectos managed). **Ajustes pre-ejecución del orquestador aplicados**: (1) `RESEND_REPLY_TO_ADDRESS` env var con default `noreply@mail.consultora-demo.test-ia.cloud` permite a Lautaro override sin redeploy si quiere reply-to específico (ej `soporte@…`); (2) footer unsubscribe HTML + text en template apunta a `/settings/notificaciones` — URL placeholder pre-T-035 (da 404 hasta T-035), aceptable MVP por anti-spam-flagging + UX normalization + data privacy compliance preemptivo; (3) defensa `if (!event.created_by) → log skipped NO_RECIPIENT + return 200` en el endpoint cubre edge case `auth.users` deleted con cascade `ON DELETE SET NULL` en `calendar_events.created_by` — el reminder ya fue claim-eado por el cron (status='sent'), no se puede revertir pero al menos no crashea + queda trazado el motivo. **Canales stub T-033/T-034**: el dispatcher hace `switch/case` por channel; senders telegram/push retornan `{ok:false, errorCode:'NO_CHANNEL_IMPL_T033'}` / `'NO_CHANNEL_IMPL_T034'` que el dispatcher mapea a `status='skipped'` (no `'failed'` — es expected hasta los tickets dedicados) + log row con error_code descriptivo para grep futuro. **Hotfix Dockerfile mid-deploy** (PR `#72` merge SHA `3e20f9c`): CC olvidó agregar las 4 env vars nuevas como `ARG` + `ENV` entries en el stage `builder` del `Dockerfile`. EasyPanel pasaba las vars con `--build-arg` correctamente pero Docker descarta variables no declaradas en el stage que las usa → `pnpm build` en el container rompe con `Invalid environment variables — ver logs arriba` mid-collecting page data /_not-found. **Fix**: PR #72 con +4 ARG + 4 entries en el ENV block del stage builder. **CI no detectó el bug** porque corre `pnpm build` directo en Ubuntu runner sin Docker multi-stage; las env vars del job venían del `env:` de `.github/workflows/ci.yml` (que CC sí actualizó con placeholders patron Sentry tras el primer fallo de CI en el PR #71). Lección: la matriz de validación pre-merge requiere chequear `Dockerfile` cuando se suman env vars required en `src/env.ts`. **Bug productivo encontrado durante smoke en VPS**: dominio Resend mostraba badge "Verified" en el dashboard pero el primer envío real falló con `RESEND_VALIDATION_ERROR`. Timeline observado: Domain Added 09:59 AM → DNS Records Verified 19:28 → Domain Fully Verified 19:29 — el badge inicial verde aparece cuando Resend valida los DNS records (`SPF`+`DKIM`+`DMARC` resuelven) pero la verificación completa en el backend del provider (que habilita el sender efectivo para envíos reales) toma ~4 min más. **NO es bug del código** — race entre DNS check inicial y backend verification. Mitigación: esperar 5+ min post-badge verde antes del smoke productivo. Documentado en `docs/operations/resend-setup.md`. **Tests** (+39 unit + 31 integration): 22 unit `email-templates-reminder.test.ts` (subject offset=0 vs >0 + HTML contiene titulo+fecha es-AR+link+footer prefs+preheader mso-hide + text fallback plain + XSS escape `<` `>` `"` `'` `&` + tipo labels es-AR fallback "Vencimiento"); 11 unit `notifications-dispatch.test.ts` (mocks `sendEmail/sendTelegram/sendPush` + admin DB mock con `from('notification_log').select.eq.eq.eq.maybeSingle` chain + 11 escenarios: happy path 3 canales / ALREADY_SENT idempotency / DISABLED skip sin log / MUTED skip / mute expirado / default enabled email / telegram/push default disabled / Resend error → failed / NO_RECIPIENT_EMAIL / NO_CHANNEL_IMPL_T033 stub mapping); +6 unit `env.test.ts` (RESEND_API_KEY required + RESEND_FROM_ADDRESS email format + INTERNAL_CRON_SECRET min 32 chars + RESEND_REPLY_TO_ADDRESS default sensato + override + reject no-email); 9 integration `notification-channel-prefs-rls.test.ts` (trigger AFTER INSERT crea row default email-enabled + backfill no duplica con ON CONFLICT DO NOTHING + SELECT propias OK + SELECT ajenas vacío + UPDATE propio OK + UPDATE ajeno bloqueado RLS + INSERT propio nuevo canal OK + INSERT ajeno bloqueado RLS + DELETE default-deny); 7 integration `notification-log-rls.test.ts` (SELECT same-tenant member OK + SELECT cross-tenant null + INSERT authenticated bloqueado default-deny + INSERT service-role OK + UPDATE trigger inmutabilidad incluso service-role + DELETE trigger inmutabilidad + UPDATE authenticated bloqueado RLS antes que trigger); 5 integration `notifications-cron-rpc.test.ts` (rpc directo via `supabase.rpc('process_pending_reminders')` con setup Vault via `set_cron_vault_secret` redirigiendo `cron_dispatch_base_url` a `http://localhost:99` blackhole — pg_net es async no espera response, el UPDATE 'sent' ya ocurrió + JOIN filter event.status='cancelled' NO procesa + event.status='completed' NO procesa + future scheduled_at NO procesa + limit 100/tick: 105 reminders → primer tick 100 sent, segundo tick 5 sent); 10 integration `dispatch-reminder-endpoint.test.ts` con `vi.mock('@/shared/notifications/resend')` patrón T-020 (auth sin header / header inválido → 401 / body no-JSON 400 / reminder_id no UUID 400 / reminder_id inexistente 404 / happy path event pending + Resend OK → 200 + notification_log row sent + provider_message_id `rsd_test_*` / event cancelled → log skipped EVENT_NOT_PENDING / event.created_by null → log skipped NO_RECIPIENT con error_detail "created_by IS NULL" / idempotency 2 POSTs → segundo skipped ALREADY_SENT sin re-invocar Resend mock / Resend error → log failed RESEND_VALIDATION_ERROR). **Cifras CI** del merge SHA `e05c537` (run [25921951018](https://github.com/LautiRoveda/consultora-demo/actions/runs/25921951018)): **339 unit/component + 56 E2E + 1 skipped = 395 tests verdes en CI** (integration corre solo local: +31 nuevos → suite full local **283 integration**). Sin nuevas shadcn components. **Setup operativo Lautaro**: documentado en `docs/operations/resend-setup.md` (cuenta Resend free 100/día → 50k/mes Plan Pro USD 20/mes + DNS records SPF+DKIM+DMARC en subdominio `mail.consultora-demo.test-ia.cloud` via Hostinger panel + API key full access + cargar `cron_dispatch_secret` real en Vault via `set_cron_vault_secret` matching `INTERNAL_CRON_SECRET` env var de EasyPanel + 4 env vars en EasyPanel + redeploy + smoke productivo 7 pasos) + `docs/operations/cron-secret-rotation.md` (procedimiento para rotar el cron secret cada 6 meses o tras compromiso). **Smoke productivo confirmado** post-merge: evento `SMOKE T-031` creado con offset=29 + `UPDATE calendar_event_reminders SET scheduled_at = now() + interval '30 seconds'` manual via Studio (truco para smoke inmediato bypassando el frontend warning hora 09:00 ART). Próximo tick del cron procesó el reminder → email real llegó a inbox `lautaroeroveda@gmail.com` con subject `[ConsultoraDemo] Vence en 29 días: SMOKE T-031`, HTML rendered correctamente con paleta indigo + CTA "Ver vencimiento en ConsultoraDemo" + footer unsubscribe + Reply-To `noreply@mail.consultora-demo.test-ia.cloud`. `notification_log` con row `channel='email' status='sent' provider_message_id='rsd_<id>'`. `calendar_event_reminders.status='sent'` + `sent_at` populado. Toda la cascade idempotency working: re-correr `process_pending_reminders()` manualmente devuelve 0 rows porque ya no hay pending due. **Sin migrations adicionales** más allá de las 2 del propio T-031. **Sin nuevas dependencies visuales** — solo `resend` server-side. **Follow-ups abiertos**: **T-033** (canal Telegram real, reemplaza stub), **T-034** (canal Web Push real, reemplaza stub), **T-035** (UI settings notificaciones — destraba el link unsubscribe del footer del email actual), **T-031-FU1** `tech-debt` (trigger `notification_log_immutable` bloquea cascade UPDATEs de FK columns `reminder_id`/`event_id` con `ON DELETE SET NULL` — descubierto durante cleanup del smoke productivo cuando intenté borrar el event de prueba; fix: refinar trigger para permitir solo cambios en FK columns whitelist `[reminder_id, event_id, recipient_user_id]`, bloqueando UPDATE de payload `status`/`provider_message_id`/`error_code`/`error_detail`/`sent_at`), **T-031-FU2** `feature` (considerar webhook Resend para tracking bounces/complaints + flag `email_invalid=true` en user post-3-bounces + fallback automático a otros canales — útil cuando vengan +50 users productivos, no crítico MVP).

### Operativo / Plataforma (transversal)

Tickets fuera del rango formal `T-001..T-078` del roadmap (`docs/technical/10-roadmap.md`), que tocan operaciones / branding / DX y no pertenecen a un módulo de negocio específico.

- **T-079** ✅ Email templates de Supabase Auth con branding ConsultoraDemo. Doc operativo en `docs/operations/email-templates.md` con los 6 HTMLs (Confirm signup + Magic Link + Reset Password + Invite User + Change Email + Reauthentication) + paleta indigo (HEX, equivalentes a tokens `globals.css`) + tipografía system stack + dimensiones (600px container) + skeleton table-based + 2 nice-to-haves heredados (preheader text con `mso-hide: all` para Outlook + `<meta name="color-scheme" content="light">` para evitar inversión en Apple Mail iOS dark mode) + tabla de variables Supabase + compat matrix + instrucciones operativas (aplicar en dashboard, test plan manual, workarounds para variables no testables sin trigger real, rollback plan, migration path a Resend). Templates aplicables via `Supabase Dashboard → Authentication → Email Templates`. Sin código en repo, sin migrations, sin tests automáticos — verificación es smoke manual disparando flow real desde `/signup`, `/login` magic, `/recuperar-password`. Subject `Confirm Your Signup` (inglés default Supabase) cambia a `Confirmá tu cuenta en ConsultoraDemo`. URL VPS `consultora-demo.test-ia.cloud` reemplaza todas las menciones del `consultora-demo.vercel.app` deprecado desde T-022.5. SMTP queda en default Supabase para trial — evaluar Resend custom SMTP si el rate limit (~30 emails/h por proyecto free tier) se vuelve cuello de botella. Cierra referencia circular pre-existente en `supabase/README.md` L161-167 (decía "wording final en el PR de T-012/T-013/T-014" y los PRs decían "wording final en supabase/README.md" — ahora ambos apuntan al doc operativo).

### RLS / multi-tenancy

**Estrategia:** ADR-0006 (shared DB + RLS + custom claim en JWT). Decisión técnica completa con tradeoffs en `docs/adr/0006-multi-tenant-rls-strategy.md`.

**Helpers SQL reusables (T-015)** en schema `public`, todas `stable security definer set search_path = ''` con grants a `authenticated` + `service_role`:

- `is_member_of_consultora(uuid) → boolean` — true si `auth.uid()` es member de la consultora.
- `is_owner_of_consultora(uuid) → boolean` — idem + `role = 'owner'`.
- `role_on_consultora(uuid) → text` — rol del user en la consultora (`'owner'` | `'member'` | `null`).
- `my_consultora_ids() → setof uuid` — consultoras donde el user es member (MVP single-tenant per user, schema soporta m2m).

**Regla forward (no negociable):** TODA policy NUEVA de tablas del dominio (T-019+ clientes, empleados, informes, EPP, …) debe usar estos helpers, NO subqueries inline a `consultora_members`. Las policies pre-T-015 ya fueron refactorizadas en `supabase/migrations/20260511131522_rls_use_helpers.sql`.

Detalle + ejemplo de uso en `supabase/README.md` sección "RLS helpers". Definición en `supabase/migrations/20260511130757_rls_helpers.sql`.

## Cómo arrancar a construir

1. Asegurate de tener instalado Node 20+, pnpm, git, GitHub CLI, Supabase CLI y Vercel CLI.
2. Abrí Claude Code en este repo.
3. Decile: *"Leé `CLAUDE.md` y los documentos de `docs/`. Después arrancamos con el ticket T-001 del roadmap."*
4. Claude propone plan, validás, codea, hace PR.
5. Reviewás, mergéas, deploy automático.
6. Pasás al siguiente ticket.

## Glosario rápido

- **HyS:** Higiene y Seguridad Laboral.
- **EPP:** Elementos de Protección Personal.
- **SRT:** Superintendencia de Riesgos del Trabajo (regulador).
- **ART:** Aseguradora de Riesgos del Trabajo.
- **RGRL:** Relevamiento General de Riesgos Laborales (anual).
- **Res 299/11:** planilla de entrega de EPP firmada, renovación cada 6 meses.
- **Decreto 351/79 / 911/96 / 617/97:** decretos reglamentarios HyS por sector.
- **Protocolo:** informe con vigencia 12 meses, debe renovarse anualmente.

Glosario completo en `docs/discovery/06-glossary.md`.

## Decisiones tomadas en discovery (no re-discutir)

- D01 · Cliente target: el consultor profesional, no el empleador final.
- D02 · Foco geográfico inicial: AMBA + Córdoba + Santa Fe.
- D03 · Foco sectorial: industria + comercio + servicios privados + construcción.
- D04 · Pitch principal: resguardo legal — secundario: productividad.
- D05 · Versionado de normas SRT con libre elección.
- D06 · Comparación de versiones de norma con IA.
- D07 · Monitoreo normativo incluido en planes pagos, diferenciado por nivel.
- D08 · Foco del producto: generación de informes + calendario de vencimientos.
- D09 · Pricing público: Pro USD 30 / Team USD 100 / Enterprise USD 250.
- D10 · Plan Team disponible desde Fase 2.
- D11 · Plan Enterprise disponible desde Fase 4.
- D12 · Foco geográfico inicial AMBA + canales orgánicos.
- D13 · Programa de referidos activo desde el día uno.
- D14 · Métricas de validación con cláusulas de pivot a 60/90/180 días.

Lista viva en `docs/discovery/00-decisiones.md`.

## Disclaimer profesional

ConsultoraDemo es un asistente que genera documentos. **El profesional matriculado es responsable de revisar y firmar todo informe antes de presentarlo legalmente.** La app no reemplaza criterio profesional ni absuelve responsabilidad civil/penal. Esto está claro en los términos de uso y en cada informe generado.
